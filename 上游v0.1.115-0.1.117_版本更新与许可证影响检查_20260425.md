# 上游 v0.1.115 / v0.1.116 / v0.1.117 更新与许可证影响检查（2026-04-25）

> 结论先行：
>
> - 当前工作区 `HEAD`（tag `v0.1.280`）的根目录 `LICENSE` 仍为 **MIT**。
> - 当前代码历史 **未包含 upstream 的 v0.1.115 / v0.1.116 / v0.1.117**（这些上游版本的 `LICENSE` 为 **LGPL-3.0**）。
> - 注意：本仓库本地也存在一个名为 `v0.1.117` 的 tag，但它与 upstream 的同名 tag **指向不同提交**（本地 `v0.1.117` 为 MIT；upstream `v0.1.117` 为 LGPL）。

## 1. 基线信息（本地仓库）

- 当前分支：`feat/admin-usage-tabs-leaderboard-request-details`（工作区 dirty）
- 当前版本描述：`v0.1.280-dirty`
- `HEAD`：`63ae057811df79140f86987388ee313249cea08e`（2026-04-25）`fix(ci): repair v0.1.280 release checks`
- Remotes：
  - `origin`：ZY4869/sub2api
  - `upstream`：Wei-Shaw/sub2api

## 2. 上游版本与许可证现状（Wei-Shaw/sub2api）

上游关键点（只列与本问题相关的版本/分支）：

- `upstream/main@0a80ec80e361f396c9c2a8963323fa093cca3068`：根目录 `LICENSE` 为 **LGPL-3.0**
- 上游 tags（`git ls-remote --tags upstream ...^{}` 的结果）：
  - `v0.1.115@755c7d5026244a587dafd3c8fe9bad45bb02d100`：`LICENSE` 为 **LGPL-3.0**，包含 `CLA.md`
  - `v0.1.116@a22a5b9e72780f6a2f5a7b2043505dd7be5e07f7`：`LICENSE` 为 **LGPL-3.0**，包含 `CLA.md`
  - `v0.1.117@a4e329c18b133e950f9345a27bfbd76624519759`：`LICENSE` 为 **LGPL-3.0**（未包含 `CLA.md`）

> 说明：由于本仓库本地已有同名 `v0.1.117` tag，我将上游 `v0.1.117` 拉取为本地 tag `upstream-v0.1.117` 用于核对，避免覆盖本地 tag。

## 3. 当前仓库是否“合入/拉取”了这些上游版本？

用 `git merge-base --is-ancestor <commit> HEAD` 判断是否“已合入历史”：

- 上游 `v0.1.115`（`755c7d50...`）不是 `HEAD` 的祖先（exit=1）
- 上游 `v0.1.116`（`a22a5b9e...`）不是 `HEAD` 的祖先（exit=1）
- 上游 `v0.1.117`（`a4e329c1...`）不是 `HEAD` 的祖先（exit=1）
- `upstream/main` 也不是 `HEAD` 的祖先（exit=1）

因此：当前代码历史并没有合入 upstream 从 `v0.1.115` 开始的那条（LGPL）主线。

补充：本仓库本地 tag `v0.1.117` 指向 `e15ff96e69200e1e73cd582bc13392e23641e5fc`，并且它是 `HEAD` 的祖先（exit=0）；但这不是 upstream 的 `v0.1.117`（同名不同提交）。

## 4. LICENSE 对比（仅截取开头用于识别）

- 本地 `HEAD:LICENSE`：以 `MIT License` 开头（Copyright (c) 2025 Wesley Liddick）
- upstream `v0.1.115/116/117:LICENSE`：以 `GNU LESSER GENERAL PUBLIC LICENSE Version 3` 开头（LGPL-3.0）

## 5. 对你 MIT 协议的影响（非法律建议）

- 仅“查看/fetch 上游 tag/commit”不会改变你仓库的许可；真正影响的是你是否把 **LGPL 代码**合并进你的发布/分发版本（merge/cherry-pick/复制源码等）。
- 目前你的 `HEAD` 历史未包含 upstream 的 LGPL 主线提交，且仓库根 `LICENSE` 为 MIT，因此就**当前状态**看，并没有因为“拉取上游 v0.1.115+”而被动变成 LGPL。
- 如果未来你把 upstream v0.1.115+（LGPL）代码合入，并对外分发/发布，通常需要遵守 LGPL 的相应义务；仅保留 `LICENSE` 为 MIT 并不能消除已引入 LGPL 代码的合规要求。

建议：若你要保持 MIT-only 的分发策略，避免直接 merge upstream v0.1.115+；如确需功能，可考虑自行实现同等功能（避免复制 LGPL 源码），或与上游作者沟通获取额外授权/双许可。涉及商业分发时，建议咨询专业法律意见。

## 6. 关键命令（留档）

```bash
git describe --tags --always --dirty

# 祖先关系（是否已合入历史）
git merge-base --is-ancestor v0.1.115 HEAD
git merge-base --is-ancestor v0.1.116 HEAD
git merge-base --is-ancestor upstream/main HEAD

# 拉取 upstream 的 v0.1.117 到一个“不会覆盖本地 tag 的名字”
git fetch upstream refs/tags/v0.1.117:refs/tags/upstream-v0.1.117
git merge-base --is-ancestor upstream-v0.1.117^{} HEAD

# LICENSE 对比
git show HEAD:LICENSE | head
git show upstream-v0.1.117^{}:LICENSE | head
```

## 7. 本地重写落地清单（对齐 upstream v0.1.115–v0.1.117）

> 说明：以下为“按 upstream 行为/接口设计参考，但本地手工重写实现”的落地清单；不包含任何 `pull/merge/cherry-pick` 的 LGPL 代码。

| 能力点 | 说明 | 状态 | 本地落点（示例） |
| --- | --- | --- | --- |
| 许可证变更 | upstream 由 MIT 切换为 LGPL-3.0；本地保持 MIT，不合入 upstream LGPL 主线 | 【不适用】 | 根 `LICENSE` 保持 MIT |
| Public Settings 增量字段 | `available_channels_enabled`、`channel_monitor_enabled`，用于 SSR/前端 gating（严格 `=== true`） | 【已重写实现】 | `backend/internal/service/setting_service_public.go`、`frontend/src/stores/app.ts` |
| Admin Settings 增量字段 | `available_channels_enabled`、`channel_monitor_enabled`、`channel_monitor_default_interval_seconds` | 【已重写实现】 | `backend/internal/handler/admin/setting_handler_general.go`、`frontend/src/views/admin/SettingsView.vue` |
| Available Channels（用户侧） | 新增用户侧聚合视图与 API：`GET /api/v1/channels/available`（开关关闭返回 `[]`） | 【已重写实现】 | `backend/internal/handler/available_channel_handler.go`、`frontend/src/views/user/AvailableChannelsView.vue` |
| Channel Monitor（用户侧） | 用户只读状态页与 API：`GET /api/v1/channel-monitors`、`GET /api/v1/channel-monitors/:id/status`（开关关闭 list=`[]`/detail=`404`） | 【已重写实现】 | `backend/internal/handler/channel_monitor_handler.go`、`frontend/src/views/user/ChannelStatusView.vue` |
| Channel Monitor（管理侧） | Admin 监控/模板 CRUD + Run/History/Apply/Associated | 【已重写实现】 | `backend/internal/handler/admin/channel_monitor_handler.go`、`frontend/src/views/admin/ChannelMonitorsView.vue` |
| Channel Monitor 数据层 | 新增监控表、历史表、聚合表与模板表（本地迁移编号 103–105） | 【已重写实现】 | `backend/migrations/103_add_channel_monitors.sql`、`backend/migrations/104_add_channel_monitor_daily_rollups.sql`、`backend/migrations/105_add_channel_monitor_request_templates.sql` |
| 模型硬移除 | 移除 `gpt-5.1*` + 旧 Codex（`gpt-5-codex`/`gpt-5.2-codex`/`gpt-5.3-codex`），请求直接报错；保留 `gpt-5.3-codex-spark*` | 【已重写实现】 | `backend/internal/service/model_hard_removed.go`、`backend/internal/modelregistry/registry_seed.json`、`backend/resources/model-pricing/model_prices_and_context_window.json` |
| API 文档同步 | `common` 页补充 Available Channels / Channel Monitor 与 public settings 开关说明 | 【已重写实现】 | `backend/internal/service/docs/pages/common.md` |

默认上线策略（防止突然暴露新入口）：

- `available_channels_enabled=false`
- `channel_monitor_enabled=false`
- `channel_monitor_default_interval_seconds=60`（并做 clamp）

