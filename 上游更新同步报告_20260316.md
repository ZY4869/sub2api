# 上游更新同步报告（2026-03-16）

> 目标：把 `upstream/main` 的最新变更拉取到本地同步分支，并给出可落地的合并（回归）计划。

## 1. 基本信息

- 同步分支：`sync/upstream-20260316`
- 上游仓库：`upstream`（Wei-Shaw/sub2api）
- 本次同步合并提交：`af52166b`
- 上游目标提交：`6447be45`（PR #1047：Codex stream isolation）
- 上游增量范围：`474165d7..6447be45`

## 2. 上游新增内容摘要（474165d7..6447be45）

该增量主要来自 PR #1047（Codex 串流隔离相关修复）：

- `ab4e8b2c`：`fix(gateway): 防止 OpenAI Codex 跨用户串流`
- `3741617e`：`fix(gateway): WS 连接池条件式 MarkBroken 防止跨请求串流`
- `6447be45`：合入 PR #1047 的 merge commit

## 3. 本地合并落点（结合当前“拆分超长文件”的代码结构）

由于本仓库当前分支对上游部分超长文件做过拆分/裁剪，本次同步并未直接“照搬上游的大文件版本”，而是把关键修复点落到现有拆分后的实现中（以保证行为一致且避免回退拆分成果）。

### 3.1 会话隔离：避免不同 API Key 复用相同 session 标识导致串流

- 新增：`backend/internal/service/openai_session_isolation.go`
  - `isolateOpenAISessionID(apiKeyID, raw)`：将 `apiKeyID` 混入 `session_id / conversation_id`，保证不同 API Key 即使传入相同原始值，上游侧也会变为不同标识，避免跨用户会话碰撞。
- 落点更新（示例性列举，均已在 merge commit `af52166b` 中体现）：
  - `backend/internal/service/openai_gateway_upstream_request.go`
  - `backend/internal/service/openai_gateway_passthrough.go`
  - `backend/internal/service/openai_gateway_chat_completions.go`
  - `backend/internal/service/openai_gateway_messages.go`
  - `backend/internal/service/openai_ws_headers.go`
- 新增单测：`backend/internal/service/openai_gateway_service_session_isolation_test.go`
- 更新现有成功链路测试：`backend/internal/service/openai_ws_forwarder_success_test.go`

### 3.2 WS 连接池：正常结束时允许复用，异常路径才 MarkBroken

- 更新：`backend/internal/service/openai_ws_forward_loop.go`
  - 对“终端事件正常退出”与“异常退出”进行区分：正常退出允许连接归还复用；异常路径（读写错误、error event、客户端断连等）会标记为 broken，避免跨请求串流风险。

## 4. 冲突与解决说明

本次与上游合并过程中 Git 记录的冲突文件：

- `backend/internal/service/openai_gateway_service.go`
- `backend/internal/service/openai_ws_forwarder.go`

解决策略（概要）：

- 维持当前分支既有的“超长文件拆分/裁剪”结构，不回退为上游的大文件形态。
- 将上游修复点（会话隔离 + 条件式 MarkBroken）映射并实现到拆分后的对应文件中（见第 3 节落点列表）。

## 5. 本次合并提交涉及文件（af52166b）

> `git show --stat -1 af52166b` 输出整理：

- backend
  - handler：`backend/internal/handler/admin/account_handler.go`、`backend/internal/handler/admin/setting_handler.go`、`backend/internal/handler/gateway_handler.go`、`backend/internal/handler/openai_gateway_handler.go`
  - repository：`backend/internal/repository/account_repo.go`、`backend/internal/repository/usage_log_repo.go`
  - service：`backend/internal/service/openai_gateway_chat_completions.go`、`backend/internal/service/openai_gateway_messages.go`、`backend/internal/service/openai_gateway_passthrough.go`、`backend/internal/service/openai_gateway_upstream_request.go`、`backend/internal/service/openai_ws_forward_loop.go`、`backend/internal/service/openai_ws_headers.go`
  - tests/new：`backend/internal/service/openai_gateway_service_session_isolation_test.go`
  - new：`backend/internal/service/openai_session_isolation.go`、`backend/internal/service/setting_service_backend_mode.go`
  - update：`backend/internal/service/setting_service_update.go`

## 6. 回归点（建议重点验证）

1. **OpenAI OAuth（ChatGPT/Codex）串流链路**
   - `session_id`、`conversation_id` 的隔离是否生效（不同 API Key → 不同上游标识）。
   - 旧客户端是否因 session 变化出现“上下文不连续”的感知（预期可接受，优先保证隔离安全）。
2. **WS 连接池复用**
   - 正常终端事件退出后连接应复用（避免每次新建导致性能下降）。
   - 任意异常路径（读写错误、error event、客户端断连）必须 MarkBroken，避免脏连接复用导致跨请求串流。
3. **透传/兼容分支**
   - compact path / 非 compact path 的 session 处理是否符合预期。
   - header 白名单/透传策略是否仍满足上游风控要求。

## 7. 合并计划（建议把本同步分支并回 main 的步骤）

> 目标：把 `sync/upstream-20260316` 的同步成果安全合入 `main`，并确保 CI/回归完整。

1. **先清理本地未提交变更**
   - 当前工作区仍存在未提交修改与新增文件（不属于 `af52166b`）：请决定“纳入本次同步并回 main”还是“另起分支/另做提交”。
   - 建议做法：把这些改动拆成独立 commit（便于 review），或先 `git stash -u` 暂存，确保 PR 只包含可控范围。
2. **跑最小回归**
   - 后端：`cd backend && go test ./...`
   - 重点关注：`backend/internal/service` 下与 WS/Codex 相关测试。
3. **推送同步分支并发起 PR**
   - `git push -u origin sync/upstream-20260316`
   - PR 标题建议：`chore(sync): merge upstream/main (2026-03-16)`
4. **代码审查清单（Review Checklist）**
   - session 隔离：是否只作用于需要隔离的场景（尤其 OAuth）且不会污染非 OAuth 账号。
   - WS 连接复用：是否存在“正常退出却被 MarkBroken”或“异常退出未 MarkBroken”的遗漏。
   - 日志/指标：关键路径是否仍可观测（不输出敏感信息）。
5. **合入 main 后的后续**
   - 若线上使用 WS/Codex：建议灰度观察错误率与连接池指标（AcquireReuse、MarkBroken、DialCount 等）。

## 8. 当前工作区未提交内容（供决策）

执行 `git status`（2026-03-16）仍显示：

- **modified**
  - `backend/internal/pkg/usagestats/usage_log_types.go`
  - `backend/internal/repository/usage_log_repo.go`
  - `backend/internal/repository/usage_log_repo_scan.go`
  - `backend/internal/repository/usage_log_repo_write.go`
  - `backend/internal/service/admin_service.go`
  - `backend/internal/service/gateway_service.go`
  - `backend/internal/service/gateway_usage_billing.go`
  - `backend/internal/service/openai_gateway_service.go`
  - `backend/internal/service/openai_gateway_usage.go`
- **untracked**
  - `backend/internal/repository/usage_log_repo_endpoints.go`
  - `backend/internal/repository/usage_log_repo_ranking.go`
  - `backend/internal/service/admin_service_group_rate.go`
  - `backend/internal/service/gateway_bedrock_beta_policy.go`
  - `backend/internal/service/setting_service_frontend_url.go`
  - `backend/internal/service/usage_billing_apply.go`
