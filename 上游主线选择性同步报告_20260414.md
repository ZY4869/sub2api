# 上游主线选择性同步报告_20260414

## 当前基线

- 审查分支：`review/upstream-main-20260414`
- 冻结修复基线：`sync/upstream-20260413@a971cec9`
- 本地对照：`main@8be6252a`
- 上游目标：`upstream/main@e534e9ba`
- 本轮策略：fix-only 主题优先吸收；重复主题先做对照卡再等你决定；纯新增主题只评估，不默认并回主线。
- 上游刷新方式：仅 `git fetch upstream main:refs/remotes/upstream/main`，不抓 tags，避免 tag clobber。

## 已吸收

- `已吸收` `68f16153` ← 上游 `b7edc3ed`
  `fix(gateway): 兼容 Cursor /v1/chat/completions 的 Responses API body`
  说明：保留本地拆分结构，手工合并到 `openai_gateway_chat_completions` 链路，并补 `openai_cursor_warmup_pipeline_test.go`。

- `已吸收` `8527068c` ← 上游 `422e25c9`
  `fix(gateway): 剥离 Cursor raw body 透传路径中 Codex 不支持的 Responses API 参数`
  说明：保留本地 `codex transform` 分层，补齐 unsupported fields 剥离与回归测试。

- `已吸收` `6856903e` ← 上游 `a1e299a3`
  `fix: Anthropic 非流式路径在上游终态事件 output 为空时从 delta 事件重建响应内容`

- `已吸收` `7130e96f` ← 上游 `6401dd7c`
  `fix(ops): increase error log request body limit from 10KB to 256KB`

- `已吸收` `7c6c62b1` ← 上游 `3a113481`
  `fix(frontend): avoid mounting hidden mobile table`

- `已吸收` `b6db8542` ← 上游 `abe42675`
  `fix(frontend): lazy load mobile account usage cells`
  说明：不回退本地 `AccountUsageCell` + `useAccountUsagePresentation` 拆分，新增 `frontend/src/composables/useViewportAutoLoadGate.ts`，仅在移动端进入视口后放行自动 usage load；桌面端继续即时加载，手动刷新也走同一视口门控。

- `已吸收` `ba9a409b` ← 上游 `c8cfad7c`
  `fix(antigravity): preserve web search alongside function tools`
  说明：本地手工移植到 `backend/internal/pkg/antigravity/request_transformer.go`，保留当前 Gemini declaration 结构。

- `已吸收` `2d22f966` ← 上游 `0ebe0ce5`、`dd5978f2`、`095bef95`、`4514f3fc`
  `fix(gemini): preserve web search and customtools aliases`
  说明：按本地分层拆成四块吸收：
  `gemini_protocol_normalize.go` 识别版本化 `web_search_*`
  `gemini_messages_request_normalize.go` 仅在 AI Studio 出站时把 `googleSearch` 归一到 `google_search`
  `account.go` / `account_wildcard_test.go` 补齐 `gemini-3.1-pro-preview-customtools` 映射与支持判断
  `pkg/gemini/models.go` 补齐 fallback catalog 的 customtools 模型元数据

- `已吸收` `1bcd27c7` ← 上游 `f00351c1`
  `fix(openai): sanitize empty base64 input images`
  说明：按本地拆分结构新增 `openai_gateway_input_image_sanitize.go`，同时覆盖 normal forward 与 passthrough 两条路径。

- `本地等价` 上游 `936fce68`
  `fix(apicompat): skip empty base64 image URLs`
  说明：本地 `backend/internal/pkg/apicompat/chatcompletions_to_responses.go` 已有 `isEmptyBase64DataURI` 等价行为。

- `本地等价` 上游 `b9b52e74`
  `fix(sidebar): prevent version dropdown clipping`
  说明：本地 `frontend/src/style.css` 当前不存在导致裁剪的 `overflow-hidden`。

- `本地等价` 上游 `118ff85f`
  `fix: 同步 LoadFactor 到调度快照缓存`
  说明：本地 `backend/internal/repository/scheduler_cache.go` 直接缓存完整 `service.Account`，`LoadFactor` 已自然进入快照，不存在上游旧 metadata 结构漏抄字段的问题。

- `功能不存在` 上游 `649afef5`
  `fix(handler): fallback known gemini models on v1beta 404`
  说明：本地 `GeminiV1BetaGetModel` 已不再转发上游模型详情接口，当前架构下没有对应补丁入口。

- `延期/跳过` 上游 `f9f57e95`
  `fix(migrations): add 097 to restore settings.updated_at default`
  说明：上游根因是历史实例曾通过 ent auto-migration 建出 `settings` 表，`005_schema_parity.sql` 的 `CREATE TABLE IF NOT EXISTS` 不会回填 SQL `DEFAULT NOW()`，后续原始 `INSERT` 才会触发 `updated_at` 非空失败。审计本地后确认：
  `backend/internal/repository/migrations_runner.go` 统一走嵌入 SQL 迁移，不依赖 ent 自动建表
  `backend/migrations/005_schema_parity.sql` 已声明 `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
  `backend/ent/schema/setting.go` 仍保留 `Default(time.Now)` / `UpdateDefault(time.Now)`
  `backend/internal/repository/setting_repo.go` 的 `Set` / `SetMultiple` 都显式写入 `updated_at`
  `rg` 未发现本地依赖 SQL DEFAULT 的 `settings` 原始插入路径
  结论：当前没有可复现缺陷，本轮维持 `延期/跳过`，不追加本地 `095` 迁移。

- `功能不存在` 上游 `24f0eebc`
  `fix(frontend): lower QR code error correction level to reduce density`
  说明：当前本地分支没有 `frontend/src/components/payment/*` 二维码组件，随 payment 专题评估，不单独拉入。

- `回归通过`
  `go test -tags=unit ./internal/pkg/antigravity -run TestBuildTools_PreservesWebSearchAlongsideFunctions`
  `go test -tags=unit ./internal/pkg/gemini -run TestDefaultModels_ContainsImageModels`
  `go test -tags=unit ./internal/service -run "TestConvertClaudeMessagesToGeminiGenerateContent_VersionedWebSearchTool|TestNormalizeGeminiRequestForAIStudio_GoogleSearchKey|TestGeminiCompatGatewayServiceForward_NormalizesWebSearchToolForAIStudio|TestAccountIsModelSupported|TestAccountGetMappedModel|TestAccountResolveMappedModel|TestSanitizeEmptyBase64InputImagesInOpenAIRequestBodyMap|TestSanitizeEmptyBase64InputImagesInOpenAIBody|TestMapAntigravityModel_.*"`
  `cmd /c "cd /d frontend && pnpm exec vitest run src/components/account/__tests__/AccountUsageCell.spec.ts --reporter=verbose"`
  `cmd /c "cd /d frontend && pnpm exec vitest run src/components/account/__tests__/AccountUsageCell.kiro.spec.ts --reporter=verbose"`
  `cmd /c "cd /d frontend && pnpm exec vitest run src/components/account/__tests__/AccountUsageResetCell.spec.ts --reporter=verbose"`
  `cmd /c "cd /d frontend && pnpm run typecheck"`

## 待决策

- `待你决策` `channel / billing / platform isolation`
  上游范围：`91c9b8d0`、`29d58f24`、`0b1ce6be`、`8d03c52e`、`2555951b`、`d72ac926`、`f5764d8d`、`c5688fef`、`8154575d`、`e51c9e50`
  本地触点：`backend/internal/service/channel_service.go`、`channel_runtime.go`、`billing_service.go`、`gateway_usage_billing.go`、`backend/internal/repository/channel_repo.go`、`frontend/src/api/admin/channels.ts`
  能力重叠：本地已具备 channel CRUD、group 绑定、billing center、runtime 平台隔离、usage channel 维度与 admin channels 页面；上游增量主要集中在渠道定价校验、billing source 一致性、antigravity 跨平台泄漏修补、cc_version/CCH header 同步。
  公开契约影响：会碰 `/admin/channels` DTO、usage channel 过滤与网关计费字段，不能整体替换。
  数据层影响：本地已有 `092_remove_sora_and_add_channels.sql` 与 `093_ops_channel_dimensions.sql`，schema 已分叉，禁止直接拿上游迁移。
  观测/权限影响：会波及 usage 记录、ops channel 维度、mixed channel 风险提示和调度期 restriction logging。
  推荐策略：`保留本地并只补缺陷`
  若只补缺陷：优先复核 `f5764d8d` requested model 计费一致性、`c5688fef` antigravity cross-platform leakage、`f585a15e` channel_mapped billing override 回退、`e51c9e50` cc_version/User-Agent header 同步。

- `待你决策` `protocol compat / Responses / ChatCompletions`
  上游范围：`68f151f5`、`4321adab`、`31660c4c`、`d927c0e4`，以及后续补丁 `c729ee42`、`4feacf22`、`936fce68`、`8afa8c10`
  本地触点：`backend/internal/pkg/apicompat/*`、`backend/internal/service/openai_gateway_chat_completions.go`、`openai_gateway_messages.go`、`backend/internal/handler/openai_gateway_handler_responses.go`、`backend/internal/server/routes/gateway_route_dispatchers.go`
  能力重叠：本地已经有 OpenAI 路由兼容、Responses/ChatCompletions endpoint normalization、Cursor body compat、ops endpoint context 与 usage 链路；上游额外能力是“Anthropic 平台分组也能吃 OpenAI `/v1/responses` / `/v1/chat/completions`”。
  公开契约影响：直接改变 `/v1/responses`、`/v1/chat/completions`、stream/non-stream 返回体和错误模型，是高风险公开契约。
  数据层影响：无迁移，但会触发 usage 的 inbound/upstream endpoint 字段和 ops request trace 记录。
  观测/权限影响：必须保留本地 request trace、ops error body/context 和 claude-code 限制语义。
  推荐策略：`保留本地并只补缺陷`
  若只补缺陷：只检查 `c729ee42` system prompt 丢失、`4feacf22` array content 支持、以及后续是否还有未吸收的 compat 边角，不导入上游整套 Anthropic-openai 路由链。

- `待你决策` `OpenAI privacy / Mobile RT / passthrough / WS mode`
  子卡 A `privacy`
  上游范围：`c13c81f0`、`47a54423`、`c489f238`、`ccd42c1d`
  本地触点：`backend/internal/service/openai_privacy_service.go`、`admin_service_openai_privacy.go`、`token_refresh_service.go`、`backend/internal/handler/admin/account_handler_privacy.go`
  结论倾向：本地已有手动设置、刷新后补偿与 async privacy 能力，推荐 `保留本地并只补缺陷`，只核对“刷新失败也尝试隐私设置”和“批量创建后异步设置”两条路径是否完全等价。
  子卡 B `Mobile RT`
  上游范围：`91b1d812`
  本地触点：`frontend/src/components/account/EditAccountModal.vue`、`backend/internal/service/openai_ws_v2_passthrough_adapter.go`、`backend/internal/handler/openai_gateway_handler_websocket.go`
  结论倾向：本地已具备 OpenAI WS mode 与 plan/entitlement 相关能力，推荐 `保留本地并只补缺陷`，仅核对 plan_type 回填与账号精确匹配是否存在缺口。
  子卡 C `passthrough`
  上游范围：`73d72651`、`6b646b61`、`ce8520c9`
  本地触点：`backend/internal/service/openai_gateway_passthrough.go`、`openai_oauth_passthrough_test.go`、`frontend/src/components/account/BulkEditAccountModal.vue`
  结论倾向：本地已支持 passthrough 开关、批量编辑与 429/529 处理，推荐 `保留本地并只补缺陷`，重点核对 429 持久化和 bulk toggle 交互差异。
  子卡 D `WS mode`
  上游范围：`adbedd48`
  本地触点：`frontend/src/components/account/EditAccountModal.vue`、`backend/internal/service/openai_ws_v2/*`
  结论倾向：本地已经有更细的 per-account WS mode 字段与协议转发实现，推荐 `抛弃上游实现`。
  汇总结论：推荐整体选择 `保留本地并只补缺陷`，不做整包融合。

- `待你决策` `usage requested_model / upstream_model + ops observability`
  上游范围：`0b845c25`、`7d312822`、`7ef933c7`、`4edcfe1f`、`095200bd`、`27948c77`、`8c4a217f`、`a2418c60`、`db9021f9`、`bd8eadb7`、`ecad083f`
  本地触点：`backend/migrations/075_add_usage_log_upstream_model.sql`、`080_add_usage_log_requested_model.sql`、`082_add_ops_error_log_context_fields.sql`、`090_add_ops_request_traces.sql`、`backend/internal/repository/usage_log_repo*.go`、`ops_repo*.go`、`frontend/src/api/admin/ops/types.ts`、`frontend/src/views/admin/ops/*`
  能力重叠：本地已持久化 `requested_model` / `upstream_model` / `actual_upstream_model`，并把它们接入 usage breakdown、ops request traces、ops error detail 和前后端 DTO；上游主题在当前分支上基本是“已存在能力的早期版本”。
  公开契约影响：会影响 admin usage/ops DTO 和前端字段优先级。
  数据层影响：本地迁移号和列集已领先上游，不能回拿上游 dual-write 迁移。
  观测/权限影响：高，任何回退都可能损伤 request trace 和 error observability。
  推荐策略：`抛弃上游实现`
  若仍想补差：只允许逐项检查字段优先级与展示细节，不允许重做 usage/ops schema。

- `待你决策` `TLS fingerprint`
  上游范围：`1854050d`
  本地触点：`backend/internal/service/tls_fingerprint_profile_service.go`、`backend/internal/repository/tls_fingerprint_profile_repo.go`、`backend/internal/handler/admin/tls_fingerprint_profile_handler.go`、`frontend/src/components/admin/TLSFingerprintProfilesModal.vue`
  能力重叠：本地已具备 TLS Profile CRUD、账号绑定、随机 profile、gateway/account usage 统一解析和前端管理入口。
  公开契约影响：会改 admin TLS profile API 与账号编辑 DTO；本地现状已稳定。
  数据层影响：本地已有 `083_create_tls_fingerprint_profiles.sql`，上游是另一套 `080_create_tls_fingerprint_profiles.sql` 编号和 ent 生成产物，不能再拿。
  观测/权限影响：影响 upstream HTTP client key、账号测试与 TLS dialer 选择。
  推荐策略：`抛弃上游实现`
  若只补缺陷：仅人工核对 `json.Number` 解析、随机 profile 回退和 pre-resolve TLS profile 三个细节，不导入上游 schema/UI。

- `待你决策` `messages dispatch + 表格后端化`
  子卡 A `messages dispatch`
  上游范围：`bbc79796`、`7dc7ff22`、`9b7b3755`
  本地触点：`backend/migrations/069_add_group_messages_dispatch.sql`、`backend/internal/service/api_key_auth_cache.go`、`frontend/src/views/user/KeysView.vue`
  结论倾向：本地已有 group 级 `allow_messages_dispatch` 能力，推荐 `保留本地并只补缺陷`，只核对 repo hydration / auth cache 是否存在上游同类 bug。
  子卡 B `表格后端化`
  上游范围：`5f8e60a1`、`67a05dfc`
  本地触点：`backend/internal/pkg/pagination/pagination.go`、`backend/internal/repository/account_repo.go`、`usage_log_repo.go`、`frontend/src/views/admin/AccountsView.vue`、`frontend/src/views/admin/UsageView.vue`
  结论倾向：本地已经具备后端分页、page-size 持久化和部分虚拟滚动；上游的后端排序/搜索实现会碰大量列表 API，推荐 `保留本地并只补缺陷`，只吸收明确缺口。
  汇总结论：推荐整体选择 `保留本地并只补缺陷`。

- `待你决策` `beta 白名单策略`
  上游范围：`00a0a121`，以及相关 header 细化 `723e5401`、`4d40fb6b`、`8375094c`
  本地触点：`backend/internal/service/setting_service_beta_policy.go`、`gateway_beta_policy.go`、`gateway_bedrock_beta_policy.go`、`frontend/src/views/admin/SettingsView.vue`
  能力重叠：本地已经有通用 beta policy settings 与网关级注入逻辑；上游实现偏 Anthropic 平台专用配置和白名单型策略。
  公开契约影响：会改 admin settings DTO 与请求头注入策略。
  数据层影响：无迁移，但会影响设置项 key 和设置页表单结构。
  观测/权限影响：会改网关请求头、模型限制和审计行为，属于高敏感治理项。
  推荐策略：`保留本地并只补缺陷`
  若只补缺陷：仅检查 Anthropic 平台 beta header 组合、账号测试按类型选 beta header、Claude Code mimic 模式合并规则。

## 纯新增评估

- `延期` `payment system v2 + subscription/payment UX + QR density`
  结论：当前本地仍以 `purchase_subscription_url` 外链购买为主，缺少支付 provider、二维码组件、订单状态流转、支付回调治理和对账闭环。
  前置条件：先补支付 provider 抽象、订单生命周期、二维码组件、回调签名与幂等，再决定是否开专题。

- `延期` `OIDC login`
  结论：本地只有 Kiro 相关 OIDC/SSO 组件，没有通用 OIDC 登录入口；该主题会触达认证、用户资料、回调路由、配置管理和隐私审计。
  前置条件：统一认证入口、用户绑定策略、回调路由、provider 配置与审计策略确定后再开题。

- `建议专题引入` `admin / group / dashboard 增强`
  结论：本地已有后台页面与服务层，但上游增强点分散在多个页面和 DTO，更适合拆题吸收。
  建议拆题：`UI 交互`、`查询/筛选`、`统计视图` 三个专题，不在当前同步分支整包引入。

- `建议专题引入` `async backup`
  结论：本地已经有 `BackupService`、`DataManagementService` 和归档能力，具备承接基础，但异步任务模型与存储清理策略尚未对齐。
  前置条件：先核对任务模型、存储配置、清理策略、失败恢复链路，再决定是否专题引入。

- `延期` `endpoint config / bulk toggles / account filters`
  结论：本地已部分具备 endpoint surface、bulk toggle、账号筛选与归档能力，和上游不是“完全缺失”的关系。
  处理方式：后续按具体缺口补差，不整包搬上游 UI 或 DTO。

- `延期` `page-size persistence / virtual scroll / last-24h / refresh token stats`
  结论：本地已存在 `getPersistedPageSize()`、`DataTable` 虚拟滚动和部分 ops token 统计能力。
  处理方式：按单点体验差异拆题吸收，不作为当前同步主线的整块新增。
