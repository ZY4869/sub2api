# 上游主线选择性同步报告（2026-04-14）

## 已吸收

- 同步基线已冻结为 `sync/upstream-20260413@893ad004`，本地对照为 `main@8be6252a`。
- `main...sync/upstream-20260413` 已核对为 `0/12`，后续不再沿用旧的 `a889bde5` / “领先 9 个提交”口径。
- 已吸收基线提交固定为：
  - `65951fda` `fix(admin): exclude rate-limited accounts from active filter`
  - `eb2b2c9d` `fix(openai): do not normalize API token based accounts`
  - `db52432c` `fix(openai): do not normalize /completion API token based accounts`
  - `0ad8d89c` `fix(openai): refresh token when expires_at missing and account is rate-limited`
  - `d2598690` `fix(openai): treat 401 {"detail":"Unauthorized"} as permanent auth failure`
  - `2e137d79` `fix(openai): ensure privacy after creating oauth accounts`
  - `31cfff8e` `fix(openai): fail over passthrough 429 and 529`
  - `1e071a10` `fix(openai): rebuild output from delta events for non-streaming responses`
  - `a889bde5` `fix(anthropic): move non-Claude Code system prompts into messages`
  - `8ece6fc6` `fix(oauth): preserve runtime state during credential refresh writes`
  - `490cc668` `fix(openai): recheck runtime state before sticky reuse`
  - `893ad004` `test(channel): cover antigravity platform isolation`
- 本轮人工补差：
  - 对齐 `ecad083f` 的行为缺口，修复 ops 前端在 `requested_model` 为空时未优先显示 `upstream_model` 的问题。
  - 补充 OpenAI 隐私失败重试、刷新 enrich `plan_type`/`privacy_mode`、channel `billing_model_source` 语义的回归测试，作为“本地已覆盖”的可执行证据。

## 已跳过

- `b155bc56`：本地已等价吸收。当前链路已具备 antigravity 隐私失败后可重试、刷新后隐私确保、令牌换取后立即设置隐私。
- `f2e596f6`：本地已等价吸收。当前 OpenAI OAuth 刷新路径会走统一 enrich 流程主动回源刷新 `plan_type`。
- `cf70fb1b`：本地已吸收。CreateAccount 隐私补齐与 `disableOpenAITraining` 请求头修正均已在当前分支存在。
- `c5688fef`：本地已等价吸收。当前实现已迁到 channel runtime 层，且 `893ad004` 已覆盖 antigravity 平台隔离回归测试。
- `oauth/privacy` 主题：`ad7c1072`、`fef9259a`、`ccd42c1d` 的目标行为已被当前分支覆盖；本轮仅补测试，不补生产逻辑。
- `channel management / platform isolation` 主题：当前 runtime 层已满足“平台隔离、映射优先级、`billing_model_source` 语义不串用”的目标；本轮仅补测试，不回退到旧 `channel_service` 路径。
- `gateway / protocol / compat` 主题：当前 Responses 路由、antigravity Claude/Gemini 边界、协议能力矩阵拒绝行为未发现可复现缺口，维持跳过。
- `beta 模型白名单策略`：当前为本地策略实现，未发现必须吸收上游的稳定性/兼容性收益，本轮按“策略差异”跳过。

## 待确认

- 暂无需要因公开接口、DTO、路由、查询参数、配置结构或 migration 变更而暂停的主题。

## 风险点

- `upstream/main` 已明显前移，本轮仅处理与既有本地实现高重叠、且能在不改公开契约前提下闭环审计的主题；未纳入本轮的上游主题仍需后续分批筛选。
- `gateway / protocol / compat` 与 `beta` 主题当前结论是“未发现可复现缺口即跳过”，若后续出现明确回归样例，应以样例驱动重新开题，而不是机械扩大会审范围。
- 旧同步报告 `上游更新同步报告_20260316.md` 保留为历史记录；本轮以本文件作为新基线后的唯一续推进报告。

## 回归结果

- 变更后补跑并通过：
  - `go test ./internal/service -run "TestOpenAIOAuthService_RefreshTokenWithClientID_EnrichesPlanTypeAndPrivacyMode|TestAdminService_EnsureOpenAIPrivacy_RetriesNonSuccessModes|TestTokenRefreshService_EnsureOpenAIPrivacy_RetriesNonSuccessModes|TestGatewayChannelState_ResolveBillingModel_ChannelMappedFallsBackToRequestedWhenUnmapped|TestGatewayChannelState_ResolveBillingModel_ChannelMappedUsesSelectionWhenMapped"`
  - `cmd /c pnpm --dir frontend run test:run -- OpsErrorLogTable OpsErrorDetailModal`
- 最终总验收已按本地可执行的等价命令完成；当前环境缺少 `make`，因此实际执行为：
  - `go test -tags=unit ./...`
  - `go test -tags=integration ./...`
  - `go test ./...`
  - `python scripts/verify_ci.py backend-lint`
  - `cmd /c pnpm --dir frontend run lint:check`
  - `cmd /c pnpm --dir frontend run typecheck`
  - `cmd /c pnpm --dir frontend run test:run`
