# 上游 v0.1.122 / v0.1.123 选择性重写同步矩阵（2026-05-06）

> 目标：在保持当前仓库 `MIT` 协议与本地架构不回退的前提下，只参考 upstream `v0.1.122` / `v0.1.123` 的 MIT 版本语义，审计当前 `HEAD` 是否已覆盖，并只对缺口做本地重写补差。

## 1. 审计基线

- 当前仓库：`HEAD @ v0.1.306`（工作区有本地未提交改动）
- 参考范围：`upstream v0.1.121..v0.1.123`
- 法务边界：
  - 允许参考：`v0.1.122`（2026-03-21）、`v0.1.123`（2026-03-22），这两个 tag 的 `LICENSE` 仍为 `MIT`
  - 明确排除：`upstream/main` 在 2026-04-19 切换到 `LGPL-3.0` 后的代码与流程（含 2026-04-21 的 `CLA`）

## 2. 主题级合并矩阵

| 主题 | 上游 0.1.122 / 0.1.123 内容 | 当前本地状态 | 结论 |
| --- | --- | --- | --- |
| Copilot OAuth 设备流 | 新增 `CopilotOAuthService`、设备流创建/轮询/创建账号/重授权 | 本地已存在完整设备流、创建草稿、创建账号、重授权、运行时 token 交换与 upstream 元数据持久化 | `本地已超越` |
| Copilot APIBaseURL 可信化 | `trustedCopilotAPIBaseURL()`，只接受可信 Copilot API 域名 | 本地已接入到 `BuildAccountCredentialsWithTokenInfo()`、`BuildAccountUpstreamExtra()`、运行时 token exchange 持久化 | `已覆盖` |
| OpenAI family target URL | `responses/models` 路由扩展到 Copilot | 本地已继续扩展到 `chat/completions`、`images`、`DeepSeek` 与 `resolveOpenAICompatibleBaseURL()` | `本地已超越` |
| OpenAI token provider | Copilot 使用 GitHub token 交换运行时 token，单独 cache key / TTL | 本地已覆盖，并额外持久化 Copilot resolved upstream；本轮补 1 个 stale 分支 TTL 细节 | `需补语义` |
| Copilot /models 导入 | `/models` 探测失败时 fallback 到内建模型 | 本地已覆盖，并补了 `ResolvedUpstream`、静态目录来源、warning notice | `已覆盖` |
| Kiro 模型导入 | 早期按 Anthropic `/models` fallback | 本地已改为 `KiroBuiltinCatalog + runtime probe metadata`，避免依赖不存在的稳定枚举接口 | `明确跳过` |
| 平台归一化 | `NormalizePlatformFamily(kiro->anthropic, copilot->openai)` | 本地 service/modelregistry 两侧都已覆盖，并继续兼容 `protocol_gateway`、`baidu_document_ai` | `已覆盖` |
| OpenAI 账号测试 | OpenAI-family OAuth / API Key 根据平台与 request format 选择上游 URL，Copilot 使用默认 headers | 本地已覆盖，并补了 protocol gateway request format / quota / known-models probe / unauthorized 处理 | `本地已超越` |
| CreateAccountModal 接入 Copilot/Kiro | 创建页新增 Copilot 设备流、Kiro token import | 本地已进一步抽成 `AccountKiroAuthPanel`，支持 `OAuth + 导入 + 会员字段` | `本地已超越` |
| Kiro token import | 基础 JSON/token 解析 | 本地已覆盖，并扩展 `api_region`、会员等级与额度信息 | `本地已超越` |
| 文档 / release / LICENSE | release bump、版本号、协议文本 | 不在本次同步边界内 | `明确跳过` |

## 3. 本轮实际补差

- `需补语义`
  - `OpenAITokenProvider`：当 `Copilot` 账号在 token version stale 分支中切换到最新账号并重新交换 token 时，需要同步刷新 `copilotTokenExpiresAt`，避免缓存 TTL 回退到默认值。

- `补回归`
  - 增加 `Copilot` 相关 target URL / token provider / 账号测试回归，锁住本地现有语义。
  - 增加 `CreateAccountModal` 中 `Copilot/Kiro` finalize-step 与 reset 行为断言，避免后续同步回退。

## 4. 明确不做

- 不 pull / merge / cherry-pick 上游提交
- 不引入 `LGPL-3.0` 协议文本、`CLA` 流程或相关 GitHub Actions
- 不为了对齐上游旧实现而回退当前本地更完整的 `DeepSeek / Mixed Gateway / Kiro runtime / 账号测试 / UI` 扩展
- 不新增 migration；若后续发现 schema 缺口，需要单独立题

## 5. 留档结论

- 当前 `HEAD` 已经吸收并显著扩展了 `v0.1.122 / v0.1.123` 的核心运行时语义。
- 本轮属于“审计 + 小补差 + 测试加固”，不是版本升级或大规模功能移植。
- 保持根目录 `LICENSE` 为 `MIT` 的目标与当前实现路径兼容。
