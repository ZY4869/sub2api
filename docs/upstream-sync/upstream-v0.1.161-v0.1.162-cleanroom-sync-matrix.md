# Upstream v0.1.161-v0.1.162 Cleanroom Sync Matrix

Local baseline: current workspace version `0.1.406`. Upstream reference is `Wei-Shaw/sub2api` compare range `v0.1.161...v0.1.162`, confirmed as 114 commits. Reference anchors are `19149ca196eeae4a4482e5299dc6fa4ba0b06c8c` and `27f094e0960ebd8e52de7ff7e763c6fec2ff4057`. Any same-name local tags are intentionally ignored.

Cleanroom rules:

- No `git pull`, `fetch`, merge, rebase, or cherry-pick from `Wei-Shaw/sub2api`.
- Do not copy upstream LGPL/GPL/CLA license text, README/governance prose, logo/assets, generated files, release scripts, or migration numbering.
- Keep the root `LICENSE` MIT-only, keep README license statements MIT, and keep local version metadata unchanged.
- Do not add `/api-docs/*` or `/admin/api-docs/*` routes, pages, handlers, or runtime overrides.
- Preserve local architecture and local feature behavior by default; overlapping upstream behavior is fused into local implementation rather than copied as a parallel stack.

| Upstream behavior anchor | Local cleanroom result | Evidence | Status |
| --- | --- | --- | --- |
| Client IP settings | Added local `ClientIPSettings` with default Gin `ClientIP()` behavior and trusted-header mode only when configured. Admin APIs are `GET /admin/settings/client-ip` and `PUT /admin/settings/client-ip`; front-end card is in the security settings area. | `backend/internal/service/setting_service_client_ip.go`, `backend/internal/handler/admin/setting_handler_client_ip.go`, `backend/internal/pkg/ip/ip.go`, `frontend/src/components/settings/ClientIPSettingsCard.vue` | Cleanroom rewrite |
| Async image-batch object storage | Added a dedicated image-batch storage setting separate from backup S3. Admin APIs are `GET /admin/settings/image-batches/storage`, `PUT /admin/settings/image-batches/storage`, and `POST /admin/settings/image-batches/storage/test`; default local storage preserves current behavior, while S3/R2 secrets are encrypted and not returned to the UI. | `backend/internal/service/setting_service_image_batch_storage.go`, `backend/internal/service/image_batch_storage.go`, `backend/internal/service/image_batch_service.go`, `frontend/src/components/settings/ImageBatchStorageSettingsCard.vue` | Cleanroom rewrite |
| Grok tool prompt cache | Fused short-TTL Grok tool prompt caching into the local Grok API key, OAuth Build, and SSO route split. Cache keys include account, route, API key/user identity, session/input fingerprint, model, and tools fingerprint; explicit `prompt_cache_key` is preserved and not overwritten. | `backend/internal/service/grok_gateway_tool_cache.go`, `backend/internal/service/grok_gateway_service.go`, `backend/internal/handler/grok_gateway_handler.go`, Grok service tests | Fused |
| Grok count tokens | Grok API key and OAuth Build count tokens prefer upstream `/v1/responses/input_tokens` when available and fall back to local estimates with `estimated` metadata; SSO unsupported paths do not fake success. | `backend/internal/service/grok_gateway_count_tokens.go`, `backend/internal/service/openai_gateway_count_tokens.go` | Fused |
| API key partial update | `ip_whitelist` and `ip_blacklist` distinguish omitted fields from empty arrays: omitted fields preserve old values, while empty arrays clear them. | `backend/internal/service/api_key_service_update_fields.go`, API key update tests | Cleanroom rewrite |
| OpenAI quota error mapping | `insufficient_quota` keeps the standard OpenAI error code and type through non-stream, stream, and failover exhaustion paths instead of being generalized to a plain rate-limit error. | `backend/internal/handler/openai_gateway_handler_errors.go`, `backend/internal/handler/openai_response_failed_event.go`, OpenAI handler tests | Cleanroom rewrite |
| Failover cache billing | Cache billing marks failover only when the execution truly switches accounts, avoiding duplicate cache billing for same-account retries. | `backend/internal/handler/failover_loop.go`, `backend/internal/handler/failover_loop_test.go` | Cleanroom rewrite |
| Anthropic compatibility | Anthropic streaming `message_start` emits `stop_reason:null`; existing non-stream Content-Type fallback remains covered. | `backend/internal/pkg/apicompat/responses_to_anthropic.go`, `backend/internal/pkg/apicompat/anthropic_responses_test.go` | Cleanroom rewrite |
| Codex transform and scheduling health | Codex system prompts are de-duplicated, `call_id` stays stable with bounded length and hash suffix, and manifest/model-probe 401 results mark reauth or temporary unschedulability. | `backend/internal/service/openai_codex_transform.go`, Codex transform and probe tests | Cleanroom rewrite |
| Agent identity import scope | Agent identity import keys include team, team_id, and organization dimensions while preserving compatibility with older account dedupe. | `backend/internal/handler/admin/account_codex_import_identity.go`, `backend/internal/handler/admin/account_codex_agent_identity_import_test.go` | Cleanroom rewrite |
| Backup S3 secret safety | Backup S3 `SecretAccessKey` is not persisted from temporary credentials or failed decryption; unsafe decrypt state returns a clear error instead of silently saving. | `backend/internal/service/backup_s3_config.go`, `backend/internal/service/backup_service.go`, backup tests | Cleanroom rewrite |
| System updater cancellation | System update runs with a background/independent context and existing operation lock rather than the HTTP request context. Update source remains the local trusted `ZY4869/sub2api`; optional GitHub API token is used only for Authorization and not exposed. | `backend/internal/handler/admin/system_handler.go`, `backend/internal/service/update_service.go`, `backend/internal/repository/github_release_service.go`, system/update tests | Cleanroom rewrite |
| Local already-covered guards | Prompt audit fail-closed behavior, local Codex/OpenAI/Grok envelope parsing, and Anthropic non-stream Content-Type fallback were already locally covered; this round keeps guard tests rather than adding duplicate implementations. | Existing prompt audit, model list, Grok, and Anthropic tests | Guarded |
| Public model examples | No request path, alias path, auth, protocol compatibility, or error example shape changes require public model example template updates. The standalone docs center remains forbidden. | `backend/internal/service/model_catalog_public_example_templates.go`, `backend/internal/repository/release_guard_162_test.go` | No template change |
| Release, license, and asset exclusions | Upstream license changes, README/license prose, logo/assets, sponsor/governance files, release scripts, generated files, and upstream migration numbering are excluded. Local MIT license, local README license wording, version `0.1.406`, and trusted update source are guarded. | `LICENSE`, `README*.md`, `backend/cmd/server/VERSION`, `backend/internal/repository/release_guard_162_test.go` | Excluded / guarded |

Validation log:

- `cd backend && go test -tags unit ./internal/service -run 'Grok.*CountTokens|GrokResponsesToolPromptCache|GrokGatewayRouteMode|SanitizeGrok|BuildGrokMessages' -count=1`: passed during implementation.
- `cd backend && go test ./internal/pkg/ip ./internal/server ./internal/server/middleware ./internal/handler/admin ./internal/handler ./internal/pkg/apicompat ./internal/service ./internal/repository -count=1`: passed.
- `cd frontend && pnpm exec vitest run src/components/settings/__tests__/ClientIPSettingsCard.spec.ts src/components/settings/__tests__/ImageBatchStorageSettingsCard.spec.ts`: passed.
- `cd frontend && pnpm test:run`: passed.
- `cd frontend && pnpm typecheck`: passed.
- `cd frontend && pnpm build`: passed, with existing Vite chunk/dynamic-import warnings.
- `cd backend && go test ./internal/repository -run TestUpstream161To162CleanroomMatrixGuards -count=1`: passed.
- `git diff --check`: passed, with Git line-ending warnings on two pre-existing settings tab files.
- `docker compose -f deploy/docker-compose.yml config`, `docker compose -f deploy/docker-compose.local.yml config`, and `docker compose -f deploy/docker-compose.dev.yml config`: not executed because this Windows environment has no `docker` executable. Text review confirmed Redis/Postgres host, port, credential, pool, volume, and health-check variables remain wired in production, local, and dev compose files.
