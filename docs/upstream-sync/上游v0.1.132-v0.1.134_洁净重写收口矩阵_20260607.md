# 上游 v0.1.132-v0.1.134 洁净重写收口矩阵（2026-06-07）

## 范围与边界

- 真实 upstream：`Wei-Shaw/sub2api`，远端标签 `v0.1.132=96837dbb1f863808a76769ee3d63d09c6e749947`、`v0.1.134=785a7791bf336af2bfbd25f002afd9f6f1752d79`。
- 本地同名 tag 不作为依据；本地基线保持 `0.1.335` 与当前工作区改动优先。
- 只参考 upstream 行为、接口语义、缺陷线索和测试意图；不 `pull`、不 merge、不 cherry-pick、不复制 LGPL 源码/测试/文案/资产。
- 明确跳过 upstream README、workflow、Dockerfile 版本线、`LICENSE`、sponsor/partner 资产、Go toolchain/version bump 等发布元数据。
- 本地根 `LICENSE` 保持 MIT；不得新增 `/api-docs/*` 或 `/admin/api-docs/*`。

## Integration Map

- 功能边界：吸收安全、网关兼容、计费、调度、用量、审计修复；本地已有能力只补缺口和测试。
- 入口与触点：`/v1/*`、OpenAI/Anthropic/Gemini 兼容层、admin/user usage、admin ops、admin accounts、settings、LinuxDo OAuth、EasyPay、后台周期任务。
- API 契约：保持本地响应结构；新增/变更只通过现有 DTO、route、service、repo 分层承载。
- 数据层：本地新增迁移从 134 顺延，覆盖 deleted API key audit、TTFT sample count、ops error key prefix、用户时间索引。
- 模型调用：继续走本地 gateway/service/client；读路径不触发同步探测，只有显式 admin probe/sync 允许下游探测。
- 日志与审计：保留 requestId/correlationId；失败请求、删除 key、后台任务、支付状态同步和 ops 归因均避免 token/PII 明文。
- 权限/配额/限流：admin 端点走 adminAuth；用户失败请求只查本人；未授权 key 查询返回不可枚举行为。
- 指标与告警：接入现有 ops/error/usage 统计，补 TTFT 样本权重、失败原因、leader lock 可观测性。
- 测试与回归：覆盖安全、协议转换、计费/额度、删除用户、失败请求 UI、账号调度、前端表格基线。
- 发布与兼容：不回退版本号，不同步 upstream LGPL 许可，不引入 sponsor/partner 资产。

## 差异吸收矩阵

| 主题 | upstream 线索 | 本地状态 | 证据 |
|---|---|---|---|
| API key 名称 XSS | CWE-79 key name escaping | 已洁净重写 | `dto/mappers_api_key.go`、`api_key_mapper_last_used_test.go` |
| 未授权 key 防枚举 | CWE-204 403/404 oracle | 已洁净重写 | 用户失败请求和用量 key 校验返回 not found；`usage_handler_failed_requests_test.go` |
| 删除用户与 key 审计 | delete user api keys with user、transaction reuse | 已洁净重写 | `admin_service_users.go`、`api_key_repo_delete.go`、`134_deleted_api_key_audit.sql`、相关 delete tests |
| LinuxDo OAuth pending/email | pending 注册误入邮箱验证 | 已洁净重写 | `auth_linuxdo_oauth.go`、`auth_linuxdo_oauth_test.go` |
| 分组描述清空 | admin group clear description | 已洁净重写 | `admin_service_groups.go`、`admin_service_group_test.go`、`group_handler.go` |
| Responses/Chat/Anthropic 兼容 | reasoning-only、tool pairing、stream lifecycle、`response.failed` | 已由本地兼容层覆盖 | `pkg/apicompat`、`openai_*gateway*`、相关 gateway/apicompat tests |
| stream 字段校验 | stream field type validation | 已洁净重写 | `openai_stream_validation.go`、`openai_stream_validation_test.go` |
| OpenAI embeddings | `/v1/embeddings` gateway | 已由本地实现覆盖并补示例测试 | `openai_embeddings*`、`model_catalog_public_examples_test.go`、`publicModelCatalogExamples.spec.ts` |
| WebSocket 大请求/桥接 | oversized websocket requests、WS image bridge | 已由本地 WS/bridge 能力覆盖 | `openai_gateway_handler_websocket.go`、`openai_ws_*` tests |
| Codex/Claude client 指纹 | allowed clients、Codex/Claude mimicry | 已洁净重写 | `pkg/openai/request.go`、`setting_service_openai_allowed_clients.go`、settings UI/tests |
| EasyPay 查询 | `trade_status` 状态查询 | 已洁净重写 | `payment_service_provider_status.go`、`payment_service_query.go`、`payment_service_test.go` |
| image rate limit/error/token | capability cooldown、真实 upstream error、output token breakdown | 已由本地 image/usage/billing 能力覆盖 | `openai_images*`、`ratelimit_*`、usage billing tests |
| Codex reset window | 5h/7d reset window 对齐与 self-heal | 已由本地账号用量窗口能力覆盖 | `account_usage_service*`、`codexUsage`、`AccountUsage*` tests |
| scheduler snapshot sync | 账号状态清除后 scheduler snapshot 同步 | 已洁净重写 | account recovery/expiry/rate-limit services 与 tests |
| 周期任务 leader lock | multi-instance background job leader lock | 已洁净重写 | `periodic_job_leader_gate.go`、`periodic_job_leader_gate_test.go` |
| TTFT sample weighting | ops TTFT sample count | 已洁净重写 | `135_ops_metrics_ttft_sample_count.sql`、`ops_repo_*`、migration schema test |
| 失败请求展示 | user/admin failed requests | 已洁净重写 | `usage_handler.go`、`FailedRequestsPanel.vue`、usage API/i18n/tests |
| 已删除用户历史使用 | admin usage deleted users/API keys | 已洁净重写 | `SearchUsers IncludeDeleted`、`SearchUsageAPIKeys`、`UsageFilters.spec.ts` |
| 账号新增/编辑模型同步 | explicit admin probe/sync | 已由本地模型导入/探测体系覆盖 | `account_handler_model_import.go`、`AccountModelImportService`、相关 tests |
| 账号 5h/7d 提示 | usage window tooltip | 已由本地账号用量 UI 覆盖 | `AccountUsageCell`、`AccountUsageResetCell`、account usage tests |
| README/workflow/version/assets | release metadata、sponsors | 跳过 | 与洁净重写和 MIT-only 边界无关 |

## 验收清单

- 后端：`go test ./...`
- 前端：`pnpm --dir frontend typecheck`、`pnpm --dir frontend test:run`
- 合规：`git diff -- LICENSE` 为空；搜索 LGPL/CLA/sponsor/partner 只允许历史审计文档或本地既有资产引用。
- 本地表格：`DataTable` subtle scrollbar、账号 airy 列宽、账号表格测试保留。
- 模型示例：受 embeddings/Responses/Chat/Anthropic 影响的模型详情示例保持后端选择和前端渲染一致。

## 当前结论

- 当前工作区已进入收口态：核心行为均已通过本地模块洁净重写或本地既有能力覆盖。
- 剩余工作是运行完整验收命令、确认 LICENSE 和合规搜索结果、检查表格基线仍存在。
