# Upstream v0.1.156 Cleanroom Sync Matrix

Local baseline: current local workspace (`0.1.392`). Upstream reference is the official `Wei-Shaw/sub2api` annotated tag `v0.1.156^{}` at `12f991dde8a58e183d4bd16a87ef6fd0df714757`; the local same-name tag, if present, is intentionally ignored.

Cleanroom rules:

- No `git pull`, `fetch`, merge, rebase, or cherry-pick from `Wei-Shaw/sub2api`.
- Do not copy upstream LGPL/GPL source text, license text, README/governance text, sponsor assets, release metadata, generated files, or migration numbering.
- Keep the root `LICENSE` MIT-only, keep README license statements MIT, and keep local version metadata unchanged.
- Do not add `/api-docs/*` or `/admin/api-docs/*` routes, pages, handlers, or runtime overrides.
- Preserve local Grok OAuth/Build behavior: CLI base URL, Build model scope backfill, quota/billing snapshots, reconcile, URL helper, and account-test output cleanup remain the contract source.

| Upstream behavior anchor | Local cleanroom result | Evidence | Status |
| --- | --- | --- | --- |
| Admin account duplicate endpoint | Added `POST /admin/accounts/:id/duplicate` with admin auth, write idempotency, recovery by operation key, redacted response credentials, and frontend action/retry key reuse. Duplicate supports static credential account types only and clears runtime state before leaving the copy paused. | `backend/internal/service/admin_account_duplicate.go`, `backend/internal/handler/admin/account_handler_crud.go`, `backend/internal/server/routes/admin.go`, `frontend/src/api/admin/accounts.ts`, duplicate tests | Cleanroom rewrite |
| OpenAI Agent Identity | Added `auth_mode=agentIdentity`, Ed25519 PKCS#8 validation, AgentAssertion signing, task registration/recovery, WS connection invalidation on task change, and sensitive value redaction. Codex import accepts Agent Identity JSON while blocking credential extras from overriding private key/runtime/task fields. | `backend/internal/service/openai_agent_identity.go`, `backend/internal/handler/admin/account_codex_import_normalize.go`, `backend/internal/handler/admin/account_codex_import_merge.go`, Agent Identity tests | Cleanroom rewrite |
| Grok OAuth credential failover | Kept local Grok CLI gateway, Build scope, quota/billing and reconciliation. Added local credential failure classification for missing/revoked/transient/provider errors, durable permanent account block, transient temp-unschedulable state, and failover response semantics without changing API key or SSO paths. | `backend/internal/service/grok_credential_failure.go`, `backend/internal/service/grok_gateway_service_apikey.go`, Grok credential tests | Fused |
| Grok token provider cache/wait semantics | Already present locally: request-path refresh, token cache, refresh-lock wait, token-version stale check, and shared refresh policy are retained. | `backend/internal/service/grok_token_provider.go`, `backend/internal/service/grok_oauth_reconciliation.go` | Already present |
| OpenAI gateway task recovery | Agent Identity invalid task responses are recovered once by registering a new task and retrying with a fresh assertion. Non-Agent Identity OAuth/API key paths continue to use existing Bearer behavior. | `backend/internal/service/openai_gateway_forward.go`, `backend/internal/service/openai_ws_forward_outbound.go`, `backend/internal/service/openai_ws_headers.go` | Fused |
| First-output timeout and client disconnect failover guard | Added configurable first semantic output timeout for OpenAI streaming paths, with high-effort override support. Timeout produces retryable failover before any client-visible semantic output; client disconnects are treated as 499-style terminal conditions instead of account exhaustion. | `backend/internal/service/openai_first_output_timeout.go`, `backend/internal/service/openai_gateway_streaming.go`, `backend/internal/handler/openai_gateway_failover_guard.go`, first-output tests | Cleanroom rewrite |
| Responses Lite namespace/additional_tools migration | Added local Responses Lite detection from Codex header or WS metadata. Namespace tools are migrated from top-level `tools` into `input.additional_tools`, supported top-level function/custom/tool_search tools are preserved, unsupported hosted tools and conflicting duplicate additional tools are rejected, and WS/HTTP bridge paths normalize the same way. | `backend/internal/service/openai_responses_lite_tools.go`, `backend/internal/service/openai_gateway_forward.go`, `backend/internal/service/openai_ws_ingress_payload.go`, `backend/internal/service/openai_ws_http_bridge.go`, Responses Lite tests | Cleanroom rewrite |
| Anthropic direct chat bridge for force-chat OpenAI accounts | Added a local direct bridge for `/v1/messages` requests selected onto OpenAI protocol-gateway accounts configured for `/v1/chat/completions`. The upstream request now targets `/v1/chat/completions` while client responses remain Anthropic JSON/SSE, preserving model mapping, stop sequences, tool proxy semantics, usage, and failover handling. | `backend/internal/pkg/apicompat/chatcompletions_anthropic_bridge.go`, `backend/internal/service/openai_gateway_messages_chat_bridge.go`, `backend/internal/service/openai_gateway_messages.go`, direct bridge tests | Cleanroom rewrite |
| Scheduler snapshot/outbox health | Local scheduler snapshot already has outbox polling, event-level timeouts, account bulk-change handling with `GetByIDs`, bucket rebuild de-duping via `batchSeenKey`, watermark cleanup through `DeleteThrough`, lag/backlog-triggered full rebuild, bucket locks, and runtime schedulable filtering so disabled/deleted/stale accounts do not remain callable. A future repo-level multi-platform list query can still improve rebuild efficiency without changing behavior. | `backend/internal/service/scheduler_snapshot_service.go`, `backend/internal/service/scheduler_outbox.go`, scheduler recovery tests | Locally equivalent |
| DataTable stale virtual row cache | Added local invalidation for measured virtual row heights when the data reference, row-key sequence, columns, or `rowKey` rule changes. Existing guards for out-of-range virtual rows and direct rendering for small/disabled virtual datasets are retained. | `frontend/src/components/common/DataTable.vue`, `frontend/src/components/common/__tests__/DataTable.spec.ts` | Cleanroom rewrite |
| Keys/Groups optional ID columns and UseKey examples | Added optional `id` columns to user Keys and admin Groups tables while keeping only `key/actions` and `name/actions` fixed visible. Codex/OpenAI/Grok usage examples keep their current root and dedicated-route contracts, with focused frontend tests covering the examples. | `frontend/src/views/user/KeysView.vue`, `frontend/src/views/admin/GroupsView.vue`, `frontend/src/components/keys/UseKeyModal.vue`, focused frontend tests | Cleanroom rewrite |
| Content moderation hot-path keyword matching | Added a local compiled-rule cache for content moderation keyword and cyber-policy rules. Requests now reuse normalized comparable keywords and precomputed keyword reasons keyed by normalized rule fingerprints instead of rebuilding large rule sets on every request. | `backend/internal/service/content_moderation_keyword_cache.go`, `backend/internal/service/content_moderation_key_config.go`, content moderation cache tests | Cleanroom rewrite |
| Upstream license/README/version/release metadata | Upstream license and release metadata are excluded. Root `LICENSE`, README license text and local version remain protected by release guard tests. | `LICENSE`, `README*.md`, `backend/cmd/server/VERSION`, `backend/internal/repository/release_guard_156_test.go` | Guarded |
| Standalone API docs center | Still forbidden; no `/api-docs/*` or `/admin/api-docs/*` routes/pages/handlers are added. | `backend/internal/repository/release_guard_156_test.go`, existing router tests | Guarded |

Deferred or locally equivalent anchors:

- Repo-level scheduler multi-platform list batching remains a performance-only follow-up; current local behavior is protected by snapshot/outbox semantics above and does not require schema or API changes for v0.1.156 absorption.
- Upstream generated files, sponsor assets, COPYING/governance text, release version bump and README rewrite are explicitly skipped.

Validation commands:

- `go test -tags unit ./internal/service ./internal/handler/admin -run "Test.*DuplicateAccount|Test.*AgentIdentity|Test.*GrokCredential" -count=1`
- `go test -tags unit ./internal/pkg/apicompat ./internal/service -run "TestForwardAsAnthropic_ProtocolGatewayChatPreference|TestNormalizeOpenAIResponsesLite|TestOpenAIStreamingFirstOutputTimeout|TestContentModeration" -count=1`
- `pnpm --dir frontend test:run src/api/__tests__/admin.accounts.duplicate.spec.ts src/views/admin/__tests__/AccountsView.spec.ts src/views/user/__tests__/KeysViewConcurrencyColumn.spec.ts src/views/user/keys/__tests__/KeysTable.spec.ts src/views/admin/__tests__/GroupsView.spec.ts src/components/common/__tests__/DataTable.spec.ts src/components/keys/__tests__/UseKeyModal.spec.ts`
- `go test ./internal/repository -run "TestUpstream156CleanroomMatrixGuards" -count=1`
- `git diff -- LICENSE README.md README_CN.md README_EN.md backend/cmd/server/VERSION`
- `rg "/api-docs|admin/api-docs" backend frontend`
