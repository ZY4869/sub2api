# 上游主线选择性同步报告_20260414

## 当前基线

- 审查分支：`review/upstream-main-20260414`
- 冻结修复基线：`sync/upstream-20260413@a971cec9`
- 本地对照：`main@8be6252a`
- 上游目标：`upstream/main@e534e9ba`
- 本轮策略：fix-only 主题优先吸收；重复主题先做对照卡再等你决定；纯新增主题只评估，不默认并回主线。
- 上游刷新方式：仅 `git fetch upstream main:refs/remotes/upstream/main`，不抓 tags，避免 tag clobber。

## 已吸收

- `已吸收` `68f16153` ← 上游 `b7edc3ed`
  `fix(gateway): 兼容 Cursor /v1/chat/completions 的 Responses API body`
  说明：保留本地拆分结构，手工合并到 `openai_gateway_chat_completions` 链路，并补 `openai_cursor_warmup_pipeline_test.go`。

- `已吸收` `8527068c` ← 上游 `422e25c9`
  `fix(gateway): 剥离 Cursor raw body 透传路径中 Codex 不支持的 Responses API 参数`
  说明：保留本地 `codex transform` 分层，补齐 unsupported fields 剥离与回归测试。

- `已吸收` `6856903e` ← 上游 `a1e299a3`
  `fix: Anthropic 非流式路径在上游终态事件 output 为空时从 delta 事件重建响应内容`

- `已吸收` `7130e96f` ← 上游 `6401dd7c`
  `fix(ops): increase error log request body limit from 10KB to 256KB`

- `已吸收` `7c6c62b1` ← 上游 `3a113481`
  `fix(frontend): avoid mounting hidden mobile table`

- `已吸收` `b6db8542` ← 上游 `abe42675`
  `fix(frontend): lazy load mobile account usage cells`
  说明：不回退本地 `AccountUsageCell` + `useAccountUsagePresentation` 拆分，新增 `frontend/src/composables/useViewportAutoLoadGate.ts`，仅在移动端进入视口后放行自动 usage load；桌面端继续即时加载，手动刷新也走同一视口门控。

- `已吸收` `ba9a409b` ← 上游 `c8cfad7c`
  `fix(antigravity): preserve web search alongside function tools`
  说明：本地手工移植到 `backend/internal/pkg/antigravity/request_transformer.go`，保留当前 Gemini declaration 结构。

- `已吸收` `2d22f966` ← 上游 `0ebe0ce5`、`dd5978f2`、`095bef95`、`4514f3fc`
  `fix(gemini): preserve web search and customtools aliases`
  说明：按本地分层拆成四块吸收：
  `gemini_protocol_normalize.go` 识别版本化 `web_search_*`
  `gemini_messages_request_normalize.go` 仅在 AI Studio 出站时把 `googleSearch` 归一到 `google_search`
  `account.go` / `account_wildcard_test.go` 补齐 `gemini-3.1-pro-preview-customtools` 映射与支持判断
  `pkg/gemini/models.go` 补齐 fallback catalog 的 customtools 模型元数据

- `已吸收` `1bcd27c7` ← 上游 `f00351c1`
  `fix(openai): sanitize empty base64 input images`
  说明：按本地拆分结构新增 `openai_gateway_input_image_sanitize.go`，同时覆盖 normal forward 与 passthrough 两条路径。

- `本地等价` 上游 `936fce68`
  `fix(apicompat): skip empty base64 image URLs`
  说明：本地 `backend/internal/pkg/apicompat/chatcompletions_to_responses.go` 已有 `isEmptyBase64DataURI` 等价行为。

- `本地等价` 上游 `b9b52e74`
  `fix(sidebar): prevent version dropdown clipping`
  说明：本地 `frontend/src/style.css` 当前不存在导致裁剪的 `overflow-hidden`。

- `本地等价` 上游 `118ff85f`
  `fix: 同步 LoadFactor 到调度快照缓存`
  说明：本地 `backend/internal/repository/scheduler_cache.go` 直接缓存完整 `service.Account`，`LoadFactor` 已自然进入快照，不存在上游旧 metadata 结构漏抄字段的问题。

- `已吸收` 上游 `4feacf22`
  `fix(apicompat): support array content for system and tool messages`
  说明：保留本地 `ChatCompletionsToResponses` 转换结构，手工补齐 system/tool message 的 array content 兼容，不引入上游整套 Anthropic-openai 路由链；新增 `TestChatCompletionsToResponses_SystemArrayContent` / `TestChatCompletionsToResponses_ToolArrayContent` 本地回归。

- `功能不存在` 上游 `649afef5`
  `fix(handler): fallback known gemini models on v1beta 404`
  说明：本地 `GeminiV1BetaGetModel` 已不再转发上游模型详情接口，当前架构下没有对应补丁入口。

- `延期/跳过` 上游 `f9f57e95`
  `fix(migrations): add 097 to restore settings.updated_at default`
  说明：上游根因是历史实例曾通过 ent auto-migration 建出 `settings` 表，`005_schema_parity.sql` 的 `CREATE TABLE IF NOT EXISTS` 不会回填 SQL `DEFAULT NOW()`，后续原始 `INSERT` 才会触发 `updated_at` 非空失败。审计本地后确认：
  `backend/internal/repository/migrations_runner.go` 统一走嵌入 SQL 迁移，不依赖 ent 自动建表
  `backend/migrations/005_schema_parity.sql` 已声明 `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
  `backend/ent/schema/setting.go` 仍保留 `Default(time.Now)` / `UpdateDefault(time.Now)`
  `backend/internal/repository/setting_repo.go` 的 `Set` / `SetMultiple` 都显式写入 `updated_at`
  `rg` 未发现本地依赖 SQL DEFAULT 的 `settings` 原始插入路径
  结论：当前没有可复现缺陷，本轮维持 `延期/跳过`，不追加本地 `095` 迁移。

- `功能不存在` 上游 `24f0eebc`
  `fix(frontend): lower QR code error correction level to reduce density`
  说明：当前本地分支没有 `frontend/src/components/payment/*` 二维码组件，随 payment 专题评估，不单独拉入。

- `回归通过`
  `go test ./internal/pkg/apicompat -run "TestChatCompletionsToResponses_SystemArrayContent|TestChatCompletionsToResponses_ToolArrayContent"`
  `go test -tags=unit ./internal/pkg/antigravity -run TestBuildTools_PreservesWebSearchAlongsideFunctions`
  `go test -tags=unit ./internal/pkg/gemini -run TestDefaultModels_ContainsImageModels`
  `go test -tags=unit ./internal/service -run "TestConvertClaudeMessagesToGeminiGenerateContent_VersionedWebSearchTool|TestNormalizeGeminiRequestForAIStudio_GoogleSearchKey|TestGeminiCompatGatewayServiceForward_NormalizesWebSearchToolForAIStudio|TestAccountIsModelSupported|TestAccountGetMappedModel|TestAccountResolveMappedModel|TestSanitizeEmptyBase64InputImagesInOpenAIRequestBodyMap|TestSanitizeEmptyBase64InputImagesInOpenAIBody|TestMapAntigravityModel_.*"`
  `cmd /c "cd /d frontend && pnpm exec vitest run src/components/account/__tests__/AccountUsageCell.spec.ts --reporter=verbose"`
  `cmd /c "cd /d frontend && pnpm exec vitest run src/components/account/__tests__/AccountUsageCell.kiro.spec.ts --reporter=verbose"`
  `cmd /c "cd /d frontend && pnpm exec vitest run src/components/account/__tests__/AccountUsageResetCell.spec.ts --reporter=verbose"`
  `cmd /c "cd /d frontend && pnpm run typecheck"`

## 待决策

- `无` 当前重复主题已全部按你的决策落定，不再存在待你决策项；下列结论保留在本节留档。

- `本地等价` `channel / billing / platform isolation`
  结论：本地已由 `forwardResultBillingModel()`、`GatewayChannelState.ResolveBillingModel()`、平台作用域定价/映射测试覆盖 requested-model 计费优先级、channel_mapped 回退与 antigravity 平台隔离；`e51c9e50` 的 CCH 签名与设置项扩展属于新增治理能力，不作为本轮 fix-only 吸收。

- `已吸收` `protocol compat / Responses / ChatCompletions`
  结论：`gateway_claude_normalize.go` 已覆盖 `c729ee42` 的 system prompt `json.RawMessage` 归一化；本轮额外吸收 `4feacf22`，补齐 system/tool message 的 array content 兼容，并新增本地回归，不替换本地 OpenAI 网关主干。

- `本地等价` `OpenAI privacy / Mobile RT / passthrough / WS mode`
  结论：本地 `openai_oauth_service.go` / `openai_privacy_service.go` 已覆盖 `plan_type` 回填、刷新后自动设置 privacy、失败态重试；`openai_oauth_passthrough_test.go` 已覆盖 passthrough 429/529 持久化；WS mode 继续保留本地 per-account 语义，不引入上游整包实现。

- `本地等价` `usage requested_model / upstream_model + ops observability`
  结论：本地 usage/ops schema、DTO、request trace 与 error detail 观测链路已领先上游并覆盖其目标，按你的决策抛弃上游实现，不做回退或重构。

- `本地等价` `TLS fingerprint`
  结论：本地 TLS profile CRUD、账号绑定、随机 profile 与解析链路已完整落地，并已统一处理 `json.Number` 等兼容细节；按你的决策抛弃上游 schema/UI，不再额外同步。

- `本地等价` `messages dispatch + 表格后端化`
  结论：`api_key_repo.go` 与 `api_key_auth_cache_impl.go` 已保留 `allow_messages_dispatch` hydrate；后端分页、page-size 持久化与 `DataTable` 虚拟滚动也已在本地主干覆盖，不额外搬运上游列表改造。

- `本地等价` `beta 白名单策略`
  结论：本地已具备通用 beta policy settings、Anthropic beta 合并/剔除规则与对应测试，维持本地治理语义，不整体替换为上游白名单实现。

## 纯新增评估

- `延期` `payment system v2 + subscription/payment UX + QR density`
  结论：当前本地仍以 `purchase_subscription_url` 外链购买为主，缺少支付 provider、二维码组件、订单状态流转、支付回调治理和对账闭环。
  前置条件：先补支付 provider 抽象、订单生命周期、二维码组件、回调签名与幂等，再决定是否开专题。

- `延期` `OIDC login`
  结论：本地只有 Kiro 相关 OIDC/SSO 组件，没有通用 OIDC 登录入口；该主题会触达认证、用户资料、回调路由、配置管理和隐私审计。
  前置条件：统一认证入口、用户绑定策略、回调路由、provider 配置与审计策略确定后再开题。

- `建议专题引入` `admin / group / dashboard 增强`
  结论：本地已有后台页面与服务层，但上游增强点分散在多个页面和 DTO，更适合拆题吸收。
  建议拆题：`UI 交互`、`查询/筛选`、`统计视图` 三个专题，不在当前同步分支整包引入。

- `建议专题引入` `async backup`
  结论：本地已经有 `BackupService`、`DataManagementService` 和归档能力，具备承接基础，但异步任务模型与存储清理策略尚未对齐。
  前置条件：先核对任务模型、存储配置、清理策略、失败恢复链路，再决定是否专题引入。

- `延期` `endpoint config / bulk toggles / account filters`
  结论：本地已部分具备 endpoint surface、bulk toggle、账号筛选与归档能力，和上游不是“完全缺失”的关系。
  处理方式：后续按具体缺口补差，不整包搬上游 UI 或 DTO。

- `延期` `page-size persistence / virtual scroll / last-24h / refresh token stats`
  结论：本地已存在 `getPersistedPageSize()`、`DataTable` 虚拟滚动和部分 ops token 统计能力。
  处理方式：按单点体验差异拆题吸收，不作为当前同步主线的整块新增。
