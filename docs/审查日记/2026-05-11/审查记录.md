# 审查记录

- 今天日期：2026-05-11
- 当前执行步骤：4. WSL 深测问题的对照复审（待执行；`STEP3-20260511-001` 已修复待复审）
- 本次特定文档：`docs/审查日记/2026-05-09/问题跟踪.md`
- 今天新生成或更新的文件：
  - `docs/审查日记/2026-05-11/审查记录.md`
  - `docs/审查日记/2026-05-11/问题跟踪.md`
  - `docs/安全实施/SECURITY_IMPLEMENTATION_PROGRESS.md`
  - `security/findings/archive/source-review-findings-2026-05-09.md`
  - `security/findings/source-review-findings.md`
  - `security/findings/source-review-recheck.md`
  - `security/findings/wsl-deep-test-findings.md`
  - `backend/internal/handler/admin/account_handler_document_ai_error_test.go`
- 今天已覆盖的 `docs/审查测试/00-20` 条目：
  - `00_README_EXECUTION_ORDER.md`：done，按 README 固定 4 步闭环执行步骤 1。
  - `01_SCOPE_AND_RULES.md`：done，确认本轮只做本地源码/配置审查，不修代码。
  - `02_ENV_AND_WSL_ISOLATED_LAB.md`：done_with_note，步骤 1 不启动 WSL 主动深测。
  - `03_CODEX_CLI_MASTER_PROMPTS.md`：done，按防御性安全审查代理约束执行。
  - `04_ATTACK_SURFACE_MAP.md`：done，复核当前工作树中网关、出站、Document AI、日志与部署相关攻击面。
  - `05_LOCAL_SOURCE_REVIEW_BACKEND_AUTH_APIKEY.md`：done，复核 API Key 分组与 Document AI 访问边界。
  - `06_LOCAL_SOURCE_REVIEW_AI_GATEWAY_BILLING_UPSTREAM.md`：done，复核上游 URL 校验、redirect、Probe、failover 与读取上限。
  - `07_LOCAL_SOURCE_REVIEW_FRONTEND_DEPLOY_DATAMANAGEMENT.md`：done，复核前端账号创建提示、部署与历史 data-management 问题。
  - `08_LOCAL_SCANNERS_AND_REPORT_IMPORT.md`：done_with_note，未新增扫描报告，以源码审查和定向测试替代。
  - `09_REMOTE_TEST_LEVELS_AND_PRODUCTION_BASELINE.md`：done_with_note，本轮不做远程验证。
  - `10_REMOTE_ASSET_ENUM_AND_STATIC_REVERSE.md`：done_with_note，以本地源码与历史静态反向结论替代。
  - `11_REMOTE_AUTH_MATRIX_AND_APIKEY_LIFECYCLE.md`：done_with_note，以本地认证/API Key 源码核对替代。
  - `12_REMOTE_AI_GATEWAY_BUSINESS_LOGIC.md`：done_with_note，以本地网关业务源码核对替代。
  - `13_REMOTE_UPSTREAM_SSRF_AND_EGRESS.md`：done，确认新问题 SR-20260511-001、SR-20260511-002。
  - `14_REMOTE_NUCLEI_ZAP_API_SCANNING.md`：done_with_note，步骤 1 不做远程主动扫描。
  - `15_DEPLOYMENT_HARDENING_NGINX_DOCKER_VPS.md`：done_with_note，仅做本地配置与历史修复核对。
  - `16_LOGGING_AUDIT_FORENSICS.md`：done，recovery 脱敏定向测试通过。
  - `17_BACKUP_UPGRADE_DATAMANAGEMENTD.md`：done_with_note，未做运行态联调，保留历史结论。
  - `18_REVERSE_MAPPING_SOURCE_CONFIG.md`：done，3 条新问题已映射到源码/配置。
  - `19_FINAL_REPORT_REMEDIATION_RETEST.md`：done，已输出新一轮源码问题单。
  - `20_STANDARDS_MAPPING_CHECKLIST.md`：done，finding 已包含 OWASP/API/WSTG/ASVS 映射。
- 定向验证：
  - `go test -tags unit ./internal/repository -run "TestHTTPUpstreamSuite/TestDo_RedirectBlocked_ReturnsControlled502" -count=1 -v`：通过。
  - `go test -tags unit ./internal/service -run "TestAccountTestServiceFormatFailedTestResponseRedirectBlocked|Test.*BaseURL|Test.*BaiduDocumentAI|Test.*DocumentAI" -count=1 -v`：通过。
  - `go test -tags unit ./internal/handler/admin -run "TestProbe.*BaseURL|Test.*Probe.*Models" -count=1 -v`：通过。
  - `go test -tags unit ./internal/server/middleware -run "TestRecovery" -count=1 -v`：通过。
- 当前阻塞项：无。
- 下一步动作：按 README 进入步骤 4，执行 WSL 深测问题对照复审并生成 `security/findings/wsl-deep-test-recheck.md`。

## 恢复核对记录

- 核对时间：2026-05-11
- 核对结论：未发现 `SR-20260511-001`、`SR-20260511-002`、`SR-20260511-003` 已修复的源码痕迹。
- 关键核对：
  - `SR-20260511-001`：`backend/internal/service/upstream_redirect_blocked.go` 仍通过 `cloneRedirectBlockedHeader(resp.Header)` 克隆原始 header，未剥离 `Location` / `Refresh`。
  - `SR-20260511-002`：Document AI 的 `async_base_url` / `direct_api_urls` 仍未接入统一上游 allowlist 校验。
  - `SR-20260511-003`：Document AI 上传、base64 解码、上游响应与结果下载仍存在无显式上限的 `io.ReadAll` / 一次性解码路径。
- 流程处理：保持步骤 1 `waiting_user_fix` 暂停状态，不生成 `security/findings/source-review-recheck.md`，不进入步骤 2。
- 下一步动作：等待用户修复后，再按 `security/findings/source-review-findings.md` 执行步骤 2 对照复审。

## 二次恢复核对记录

- 核对时间：2026-05-11
- 核对结论：再次未发现 `SR-20260511-001`、`SR-20260511-002`、`SR-20260511-003` 已修复的源码痕迹。
- 关键核对：
  - `SR-20260511-001`：`backend/internal/service/upstream_redirect_blocked.go` 的 `cloneRedirectBlockedHeader` 仍直接 `return src.Clone()`，未删除 `Location` / `Refresh`。
  - `SR-20260511-002`：`validateBaiduDocumentAIAccountInput` 仍仅校验 `async_base_url` 前缀与 `direct_api_urls` 非空；运行时 `ValidateHTTPURL(..., ValidationOptions{})` 仍未传入 allowlist 约束。
  - `SR-20260511-003`：`document_ai_handler.go`、`document_ai_baidu_client.go`、`document_ai_service.go` 仍存在 `io.ReadAll` / `DecodeString` 无显式 Document AI 上限路径。
- 流程处理：继续保持步骤 1 完成后的 `waiting_user_fix` 状态；不生成新的 `security/findings/source-review-recheck.md`，不进入步骤 2。
- 下一步动作：等待用户修复三项源码问题后，再执行步骤 2 对照复审。

## SR-20260511 修复实施记录

- 执行时间：2026-05-11
- 流程定位：用户明确要求实施修复计划；本次只修步骤 1 三项 finding，不生成步骤 2 正式复审单。
- 修复摘要：
  - `SR-20260511-001`：redirect 阻断响应克隆上游 header 后删除 `Location` 与 `Refresh`，保留受控 `X-Sub2API-Upstream-Redirect-Blocked` 标记。
  - `SR-20260511-002`：新增 `security.url_allowlist.document_ai_hosts`，创建/更新/批量更新及运行时均校验 Document AI `async_base_url`、`direct_api_urls` 与结果下载 URL；用户 `file_url` 保持原有私网拦截策略。
  - `SR-20260511-003`：新增 Document AI 上传、上游 JSON、结果下载三类显式大小上限；multipart、base64、provider JSON 与结果下载均改为受限读取或解码前估算。
- 同步更新：
  - 内置 API 文档：`backend/internal/service/docs/pages/document-ai.md`、`backend/internal/service/docs/pages/common.md`
  - 配置示例：`deploy/config.example.yaml`
  - 项目说明：`README.md`、`README_CN.md`、`README_EN.md`
  - 安全流程状态：`docs/安全实施/SECURITY_IMPLEMENTATION_PROGRESS.md`、本日 `问题跟踪.md`
- 修复验证：
  - `go test -tags unit ./internal/repository -run "RedirectBlocked|HTTPUpstream" -count=1 -v`：通过。
  - `go test -tags unit ./internal/service -run "BaiduDocumentAI|DocumentAI|BaseURL|Account.*BaseURL|BulkUpdateAccounts_.*BaiduDocumentAI" -count=1 -v`：通过。
  - `go test -tags unit ./internal/handler -run "DocumentAI" -count=1 -v`：通过。
  - `go test ./internal/config -run "DefaultSecurityToggles|DocumentAI|URLAllowlist|Defaults" -count=1 -v`：通过。
  - `go test ./cmd/server -count=1`：通过。
- 当前状态：三项问题均为“已修复待复审”；下一步按 README 执行步骤 2，生成新一轮 `security/findings/source-review-recheck.md`。

## 步骤 2 源码修复对照复审启动记录

- 启动时间：2026-05-11
- 当前执行步骤：2. 源码修复后的对照复审
- 对照问题单：`security/findings/source-review-findings.md`
- 计划输出：`security/findings/source-review-recheck.md`
- 本次特定文档：`docs/审查日记/2026-05-09/问题跟踪.md`
- 已读取输入：`docs/安全实施/README.md`、`docs/安全实施/SECURITY_IMPLEMENTATION_PROGRESS.md`、`security/findings/source-review-findings.md`、两份历史审计报告、三个 README、`docs/审查测试/00-20`、既有审计产物与本次特定问题跟踪文档。
- 当前复审对象：
  - `SR-20260511-001`：blocked redirect 响应头净化。
  - `SR-20260511-002`：Document AI 出站 URL allowlist 覆盖。
  - `SR-20260511-003`：Document AI 上传、base64、上游 JSON 与结果下载大小上限。
- 暂停规则：本轮只执行步骤 2；不生成 `wsl-deep-test-findings.md`，不进入步骤 3 主动深测。

## 步骤 2 源码修复对照复审完成记录

- 完成时间：2026-05-11
- 输出文件：`security/findings/source-review-recheck.md`
- 复审结论：
  - `SR-20260511-001`：fixed。`cloneRedirectBlockedHeader` 已删除 `Location` / `Refresh`，redirect blocked 定向测试通过。
  - `SR-20260511-002`：fixed。Document AI `async_base_url`、`direct_api_urls` 与结果下载 URL 已接入 `security.url_allowlist.document_ai_hosts`；创建、更新、批量更新与运行时链路均有校验。
  - `SR-20260511-003`：fixed。Document AI multipart、JSON `file_base64`、provider JSON 与结果下载均有显式大小上限或受限读取。
- 新问题：未发现新的高优先级源码问题；`source-review-recheck.md` 中无 `new_issue`。
- 定向验证：
  - `go test -tags unit ./internal/repository -run "RedirectBlocked|HTTPUpstream" -count=1 -v`：通过。
  - `go test -tags unit ./internal/service -run "BaiduDocumentAI|DocumentAI|BaseURL|Account.*BaseURL|BulkUpdateAccounts_.*BaiduDocumentAI" -count=1 -v`：通过。
  - `go test -tags unit ./internal/handler -run "DocumentAI" -count=1 -v`：通过。
  - `go test ./internal/config -run "DefaultSecurityToggles|DocumentAI|URLAllowlist|Defaults" -count=1 -v`：通过。
  - `go test ./cmd/server -count=1`：通过。
  - 临时复审测试 `TestBaiduDocumentAIClientLimitsResultDownloadResponseRecheck`：通过；临时测试文件已删除。
- `docs/审查测试/00-20` 覆盖状态：已在 `docs/安全实施/SECURITY_IMPLEMENTATION_PROGRESS.md` 更新，步骤 2 相关项均记录为 `done` 或 `done_with_note`。
- 当前状态：步骤 2 已完成，当前步骤推进到 3。
- 下一步动作：按 README 开始步骤 3，执行 WSL 部署测试版 + 本地深度逆向/渗透/攻击验证；本轮未生成 `wsl-deep-test-findings.md`。

## 步骤 3 WSL 深度验证启动记录

- 启动时间：2026-05-11
- 当前执行步骤：3. WSL 部署测试版 + 本地深度逆向/渗透/攻击验证
- 计划输出：`security/findings/wsl-deep-test-findings.md`
- 本次特定文档：`docs/审查日记/2026-05-09/问题跟踪.md`
- 已读取输入：`docs/安全实施/README.md`、`docs/安全实施/SECURITY_IMPLEMENTATION_PROGRESS.md`、两份历史审计报告、三个 README、`backend/`、`frontend/`、`deploy/`、`tools/`、`docs/审查测试/00-20`、`security/findings/source-review-recheck.md`、历史 `wsl-deep-test-findings.md` 与本次特定问题跟踪文档。
- 边界确认：
  - 只在本地/WSL/明确授权隔离环境做主动验证。
  - 不访问生产目标，不访问真实第三方上游，不输出完整密钥、Token、Cookie、密码。
  - 发现问题后输出正式问题单并按 README 暂停等待修复。
- 初始环境观察：
  - `wsl.exe --status` 显示默认发行版为 Ubuntu，WSL 版本为 2。
  - Windows 侧未发现 `docker` 命令；下一步核对 WSL 侧 Docker 与主机服务回退路径。
- 下一步动作：准备本地/WSL 隔离测试环境并执行步骤 3 运行态验证。

## 步骤 3 WSL 深度验证完成记录

- 完成时间：2026-05-11
- 输出文件：`security/findings/wsl-deep-test-findings.md`
- 执行环境：
  - WSL 可用；Windows 侧未发现可用 `docker` 命令，标准 Docker 链路未作为稳定基线。
  - 已采用 WSL 主机服务回退路径运行当前测试版后端与 mock upstream。
  - 后端审计运行时监听 `127.0.0.1:18081`，mock upstream 监听 `127.0.0.1:19090`。
- 运行态正向控制：
  - `/health` 返回 `200`。
  - 匿名管理端/网关访问被拒绝，普通用户访问管理端被拒绝，管理员登录可用。
  - API Key 生命周期通过官方 API 复测：inactive、expired、deleted、disabled user 均受控拒绝。
  - redirect blocked 隔离复测返回受控 `502`，未透传 `Location` / `Refresh`，mock 未观察到第二跳。
  - 上游 `500` 隔离复测返回受控 `502`，未出现 `panic recovered`，未观察到测试哨兵敏感值进入服务日志。
  - Document AI base64 超限返回 `400 document_ai_invalid_request`，provider 异常返回受控 `502`。
- 本轮新问题：
  - `STEP3-20260511-001`：Document AI 非 allowlist URL 保存/批量更新被校验拒绝后，对外返回 `500 internal error`，未返回受控 `4xx`。
  - 证据：`tmp/step3_runtime_audit_20260511_summary.json`、`security/reports/manual/server-audit.log`、`backend/internal/service/admin_service_accounts.go`、`backend/internal/pkg/errors/errors.go`、`backend/internal/pkg/response/response.go`。
- 历史步骤 3 问题回归：
  - `STEP3-001`、`STEP3-002`、`STEP3-003`、`STEP3-004` 未复现，不在本轮重复登记。
- 静态与扫描说明：
  - `pnpm --dir frontend build` 在限定时间内未完成，已停止相关构建进程；本轮改用当前嵌入式 `backend/internal/web/dist` 做静态反向核对。
  - 嵌入式 dist 未发现 `.map` 文件；定向敏感串扫描未发现可用明文秘密。
  - nuclei、ZAP、trivy、semgrep、gitleaks、nmap、sqlmap 等本地工具不可用或缺失；按 `done_with_note` 记录。
- `docs/审查测试/00-20` 覆盖状态：已在 `docs/安全实施/SECURITY_IMPLEMENTATION_PROGRESS.md` 更新，每项均为 `done` 或 `done_with_note`。
- 当前状态：步骤 3 已完成，按 README 暂停等待用户修复。
- 下一步动作：用户修复 `STEP3-20260511-001` 后，执行步骤 4 并生成 `security/findings/wsl-deep-test-recheck.md`。

## 步骤 3 恢复核对记录

- 核对时间：2026-05-11
- 当前执行步骤：3. WSL 部署测试版 + 本地深度逆向/渗透/攻击验证（已完成后等待修复）
- 对照问题单：`security/findings/wsl-deep-test-findings.md`
- 本次特定文档：`docs/审查日记/2026-05-09/问题跟踪.md`
- 已按恢复流程读取输入：`docs/安全实施/README.md`、进度文件、两份历史审计报告、三个 README、`backend/`、`frontend/`、`deploy/`、`tools/`、`docs/审查测试/00-20`、步骤 1/2/3 既有审计产物与本次特定问题跟踪文档。
- 核对结论：`STEP3-20260511-001` 暂未修复，不进入步骤 4。
- 关键证据：
  - `backend/internal/service/admin_service_accounts.go` 中 `newBaiduDocumentAIValidationError` 仍返回普通 `errors.New(reason)`。
  - `backend/internal/pkg/errors/errors.go` 对普通 error 仍兜底为 generic internal error。
  - `backend/internal/pkg/response/response.go` 仍会将该兜底错误输出为 `500 internal error`。
- 流程处理：保持 `docs/安全实施/SECURITY_IMPLEMENTATION_PROGRESS.md` 的当前总状态为 `waiting_user_fix`，不生成 `security/findings/wsl-deep-test-recheck.md`。
- 下一步动作：等待用户修复 `STEP3-20260511-001` 后，再执行步骤 4 WSL 深测问题对照复审。

## 步骤 3 二次恢复核对记录

- 核对时间：2026-05-11
- 当前执行步骤：3. WSL 部署测试版 + 本地深度逆向/渗透/攻击验证（已完成后等待修复）
- 对照问题单：`security/findings/wsl-deep-test-findings.md`
- 核对结论：`STEP3-20260511-001` 仍未修复，不进入步骤 4。
- 关键证据：
  - `backend/internal/service/admin_service_accounts.go:681` 仍为 `return errors.New(reason)`。
  - 当前错误链路仍会把普通 error 兜底为 `500 internal error`。
- 流程处理：继续保持 `waiting_user_fix`；不生成或覆盖本轮 `security/findings/wsl-deep-test-recheck.md`。
- 下一步动作：等待用户将 Document AI 账号校验失败改为统一应用错误类型并映射到受控 `4xx` 后，再执行步骤 4。

## STEP3-20260511-001 修复实施记录

- 执行时间：2026-05-11
- 流程定位：用户明确要求“修复所有有问题内容”；本次修复步骤 3 正式问题单中唯一未关闭项 `STEP3-20260511-001`，不生成步骤 4 正式复审单。
- 修复摘要：
  - `backend/internal/service/admin_service_accounts.go`：新增稳定 reason `ACCOUNT_INVALID_DOCUMENT_AI_CREDENTIALS`，`newBaiduDocumentAIValidationError` 改为返回统一 `infraerrors.BadRequest(...)`，不再返回普通 `errors.New(reason)`。
  - `backend/internal/pkg/response/localization.go`：补充 `ACCOUNT_INVALID_DOCUMENT_AI_CREDENTIALS` 本地化文案。
  - `backend/internal/handler/admin/account_handler_document_ai_error_test.go`：新增创建与批量更新 handler 测试，确认 Document AI 校验失败返回 `400` 且响应不含 `internal error`。
  - `backend/internal/service/admin_service_account_base_url_validation_test.go`、`backend/internal/service/admin_service_bulk_update_test.go`：补强非 allowlist `async_base_url` / `direct_api_urls` 的结构化 `BadRequest` 断言。
  - `backend/internal/service/docs/pages/common.md`、`backend/internal/service/docs/pages/document-ai.md`：同步说明 Document AI 账号 URL / 凭证校验失败返回 `400 ACCOUNT_INVALID_DOCUMENT_AI_CREDENTIALS`。
- 定向验证：
  - `go test -tags unit ./internal/service -run "BaiduDocumentAI|BulkUpdateAccounts_.*BaiduDocumentAI|Account.*BaseURL" -count=1 -v`：通过。
  - `go test -tags unit ./internal/handler/admin -run "DocumentAIValidationError|Localization|TouchedAccount" -count=1 -v`：通过。
  - `go test ./internal/pkg/response -run "Localization|ErrorFrom" -count=1 -v`：通过。
- 当前状态：`STEP3-20260511-001` 已修复待步骤 4 运行态复审；未执行 WSL 复测，未生成 `security/findings/wsl-deep-test-recheck.md`。
- 下一步动作：按 README 执行步骤 4，对照 `security/findings/wsl-deep-test-findings.md` 做 WSL 深测问题复审。

## 步骤 4 续跑启动记录

- 启动时间：2026-05-11
- 当前执行步骤：4. WSL 深测问题的对照复审
- 对照问题单：`security/findings/wsl-deep-test-findings.md`
- 本次特定文档：`docs/审查日记/2026-05-09/问题跟踪.md`
- 本轮复审范围：仅复审 `STEP3-20260511-001`，并确认默认百度 Document AI allowlist endpoint 创建/更新未被误伤
- 启动前已核对输入：
  - `docs/安全实施/README.md`
  - `docs/安全实施/SECURITY_IMPLEMENTATION_PROGRESS.md`
  - `docs/security_audit_20260324.md`
  - `docs/security_audit_20260508.md`
  - `security/SECURITY_SCOPE.md`
  - `security/DEPLOYMENT_MAP.md`
  - `docs/审查测试/00-20`
  - `security/findings/wsl-deep-test-findings.md`
  - `docs/审查日记/2026-05-11/问题跟踪.md`
  - `docs/审查日记/2026-05-09/问题跟踪.md`
- 启动动作：
  - 已将进度文件切换为步骤 4 `in_progress`
  - 已准备归档历史 `security/findings/wsl-deep-test-recheck.md`
  - 正在校验 `backend/bin/server-test-linux` 是否足以证明包含 `ACCOUNT_INVALID_DOCUMENT_AI_CREDENTIALS` 修复；若不能证明则先重建 Linux 审计二进制
- 今天已覆盖的 `docs/审查测试/00-20` 条目补充：
  - `00_README_EXECUTION_ORDER.md`：done，按四步闭环从现有进度继续步骤 4，不回退步骤 1-3
  - `01_SCOPE_AND_RULES.md`：done，本轮仅在本地授权 WSL/主机环境复审，不访问 production/staging
  - `02_ENV_AND_WSL_ISOLATED_LAB.md`：done_with_note，继续沿用 WSL 主机服务回退链路；必要时重建 Linux 审计二进制
  - `03_CODEX_CLI_MASTER_PROMPTS.md`：done，按防御性安全审查代理约束执行步骤 4 续跑
  - `11_REMOTE_AUTH_MATRIX_AND_APIKEY_LIFECYCLE.md`：done，本轮只复用管理员登录与本地账号创建链路，不扩展矩阵
  - `12_REMOTE_AI_GATEWAY_BUSINESS_LOGIC.md`：done，本轮只复审 Document AI 创建/批量更新错误语义与 allowlist 正向链路
  - `13_REMOTE_UPSTREAM_SSRF_AND_EGRESS.md`：done，将复测非 allowlist URL 返回受控 `400 ACCOUNT_INVALID_DOCUMENT_AI_CREDENTIALS` 且无非预期出站
  - `16_LOGGING_AUDIT_FORENSICS.md`：done，将核对 `server-audit.log` 与 mock 日志无敏感值泄露
  - `18_REVERSE_MAPPING_SOURCE_CONFIG.md`：done，步骤 4 将补齐 `STEP3-20260511-001` 最新运行态复审证据
  - `19_FINAL_REPORT_REMEDIATION_RETEST.md`：done，本轮目标是生成新的 `security/findings/wsl-deep-test-recheck.md`
  - `20_STANDARDS_MAPPING_CHECKLIST.md`：done，继续按 API 错误处理、出站治理、日志脱敏与授权边界标准复审
- 当前阻塞项：无
- 下一步动作：完成 Linux 审计二进制校验后，执行 Document AI 定向测试与运行态 replay

## 步骤 4 续跑完成记录

- 完成时间：2026-05-11
- 输出文件：
  - `security/findings/archive/wsl-deep-test-recheck-2026-05-10.md`
  - `security/reports/manual/2026-05-11-step4-document-ai-recheck.md`
  - `security/findings/wsl-deep-test-recheck.md`
  - `tmp/step4_document_ai_recheck_20260511_summary.json`
- 预检结论：
  - `backend/bin/server-test-linux` 文件时间为 `2026-05-11 14:30`
  - 已在二进制中命中 `ACCOUNT_INVALID_DOCUMENT_AI_CREDENTIALS` 与相关内置文档字符串
  - 本轮无需重建 Linux 审计二进制
- 定向验证：
  - `cd backend && go test -tags unit ./internal/service -run "BaiduDocumentAI|BulkUpdateAccounts_.*BaiduDocumentAI|Account.*BaseURL" -count=1 -v`：通过
  - `cd backend && go test -tags unit ./internal/handler/admin -run "DocumentAIValidationError|Localization|TouchedAccount" -count=1 -v`：通过
  - `cd backend && go test ./internal/pkg/response -run "Localization|ErrorFrom" -count=1 -v`：通过
- 运行态 replay 结论：
  - Document AI 非 allowlist URL 保存：`400 ACCOUNT_INVALID_DOCUMENT_AI_CREDENTIALS`
  - Document AI 非 allowlist URL 批量更新：`400 ACCOUNT_INVALID_DOCUMENT_AI_CREDENTIALS`
  - 默认百度 allowlist endpoint 保存：`200`
  - 默认百度 allowlist endpoint 批量更新：`200`
  - 负向响应未出现 `internal error`
  - `mock_request_count = 0`，保存面拦截未触发非预期出站
- 日志核对：
  - 已命中 `baidu document ai account validation failed`
  - access log 中 `POST /api/v1/admin/accounts` 和 `POST /api/v1/admin/accounts/bulk-update` 负向请求均为 `400`
  - 未观察到管理员 token、本轮 `Cookie` / `X-Client-Request-Id` 哨兵值或 `internal error`
- `docs/审查测试/00-20` 本轮补充结论：
  - `00_README_EXECUTION_ORDER.md`：done，严格按现有进度续跑步骤 4 并完成
  - `01_SCOPE_AND_RULES.md`：done，仅在本地 WSL / 主机授权环境复审
  - `02_ENV_AND_WSL_ISOLATED_LAB.md`：done_with_note，沿用现有 WSL host runtime，未重建二进制
  - `03_CODEX_CLI_MASTER_PROMPTS.md`：done，先做二进制/测试/运行态证据再更新正式问题单
  - `11_REMOTE_AUTH_MATRIX_AND_APIKEY_LIFECYCLE.md`：done，仅复用管理员登录与本地账号创建
  - `12_REMOTE_AI_GATEWAY_BUSINESS_LOGIC.md`：done，负向 `400` / 正向 `200` 均已验证
  - `13_REMOTE_UPSTREAM_SSRF_AND_EGRESS.md`：done，非 allowlist URL 被保存面拦截，未发生非预期出站
  - `16_LOGGING_AUDIT_FORENSICS.md`：done，日志无敏感值泄露
  - `18_REVERSE_MAPPING_SOURCE_CONFIG.md`：done，最新复审证据已映射到 service / response / access log
  - `19_FINAL_REPORT_REMEDIATION_RETEST.md`：done，已生成新的步骤 4 正式复审单
  - `20_STANDARDS_MAPPING_CHECKLIST.md`：done，已按 API 错误处理、出站治理、日志脱敏和授权边界完成复审
- 当前复审结论：
  - `STEP3-20260511-001`：fixed
- 当前阻塞项：无
- 下一步动作：步骤 4 已完成；当前四步闭环已全部完成，默认停止在复审产物与状态文件更新处

## 最终总审计报告补充记录

- 记录时间：2026-05-11
- 新生成文件：
  - `security/security-audit-report.md`
- 报告范围：
  - 汇总步骤 1 源码问题单、步骤 2 源码复审单、步骤 3 运行态问题单、步骤 4 运行态复审单
  - 汇总攻击面地图、认证矩阵、静态反向分析与本轮 manual report
- 当前状态：
  - 最终总审计报告已补齐
  - 四步闭环与最终汇总产物均已完成
- 下一步动作：
  - 无；等待用户下一步指令
