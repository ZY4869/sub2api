# v0.1.419 发布记录

日期：2026-09-09。基线：`v0.1.418` / `651b341f6be87d7db082e9f8d16cefd2cfb3a68c`。

## 范围与授权

用户要求将当前所有应入库改动提交并推送 tag、更新版本，持续监测编译；失败后修复并重新推送，仅剩 Release 运行时停止监测。

目标仓库为 `ZY4869/sub2api`，分支为 `sync/upstream-v0.1.136-selective-cleanroom`。本次发布包含任务开始时已有的全部 143 个变更文件，经官方 `go mod tidy` 清除生成器留下的多余校验记录后，`go.sum` 与基线一致，无依赖升级。新增版本同步、忽略规则与本记录。提交、创建 tag 和普通 Push 属于本次授权范围；不执行部署、数据库迁移或历史改写。

## Integration Map

| 入口与链路 | 变更及验证边界 |
|---|---|
| 账号表 → 原账号 API → 详情弹窗 | 响应式列折叠、详情信息、品牌标识；前端类型、测试、Lint 与构建验证 |
| 每日 5H 设置 → 调度器 → 既有请求转发 → 账号 extra | 北京时间、分页、自动选模、重试及结果展示；后端单元与集成回归 |
| 模型种子 → 注册表生成器 → 本地策略投影 → 模型列表/详情 | 模型目录、价格、协议与公开示例同步；保留白名单优先和仅公开 display_model_id 的约束 |
| 分组设置 → DTO/服务 → Ent/数据库 → HTTP/WebSocket 转发与计费 | 推理超限 downgrade/deny、强制/免费 OpenAI Fast；迁移 173/174 增加三列，默认保持旧行为 |
| Kimi 账号 → 平台协议适配 → 模型选择/上游地址 | 复用现有 OpenAI 兼容路径及策略，相关示例与测试同步 |
| VERSION/package.json/release guard → tag → GitHub Actions | 版本统一为 0.1.419；CI、Security Scan、Release 按仓库现有工作流运行 |

## 发布前处理与验证

- `.gitignore` 补充旧 pnpm 依赖目录、WSL 运行数据和本地后端构建目录；既有环境配置、数据库文件、依赖、日志与缓存继续排除。
- ✅ `go -C backend mod tidy` 完成，使用 Go 1.26.6；不手改锁文件或生成代码。
- ✅ 前端类型检查、全量 Lint（排除本地旧依赖目录）及生产构建通过。
- ❌ 首轮前端全量测试 1470 通过、7 失败；涉及 Kimi 平台顺序、模型曝光和响应式列合并的旧断言。已同步新行为并加强不可测试模型排除、Kimi 点击事件和重置时间保留断言；✅ 修复后相关 5 个文件 41 项测试通过。
- ❌ 首轮后端单元测试发现两处模型回归：Haiku 旧 ID 的 deprecated 替代状态被种子刷新覆盖，以及管理端曝光测试依赖旧模型列表。恢复本地替代语义并使用显式 runtime-only 测试数据；✅ 两项定向回归通过，模型快照通过原生成器重建。
- 后端全量结果另外暴露每日 5H API 契约缺少默认 `trigger_time`、7 个既有 OpenAI 模型丢失 `image_generation_tool`。已补齐契约并恢复能力标记，将图片路由测试扩展至全部 7 个模型；保留原错误语义和 `/v1/responses` 提示。
- ✅ 图片路由与模型刷新定向回归通过；每日 5H GET 校验默认 07:00，PUT 校验自定义 08:30。管理端与模型注册表两包全量回归通过。
- 对 6 个本次已修改的 Go 源文件运行 `gofmt`，保持 CI 格式要求。
- ❌ Windows 仓库源码守卫测试在文件遍历中触发默认 10 分钟超时；未降低断言或放宽超时，Linux CI 继续完整核验。
- 后端全量与相关包回归结果，以及本地验证日志保存在 `.cache/release-v0.1.419-20260909-103307/`；跨平台完整验证由 GitHub Actions 继续执行。
- ✅ 变更内容的常见密钥格式扫描未命中；`git diff --check` 通过。
- 云端状态以对应 tag 和提交 SHA 的 GitHub Actions 记录为准；仅剩 Release 运行时依用户要求结束监测，不将其记为发布成功。
- ⚪ 不执行生产环境、真实供应商调用、实际定时调度和部署验收。

## 备份、风险与回退

工作区原始文件、二进制 diff 和基线清单已保存到 `.cache/release-v0.1.419-20260909-103307/`。旧提交与旧 tag 保留。失败修复追加提交并递增 tag，不覆盖已发布版本。

本次推送会触发发布制品和镜像，但不更新运行中的服务。后续部署前应备份数据库；新增迁移涉及分组设置，不能把制品回退等同于数据库回退。需要回退时可选择旧版制品；新增列保留，数据库任何回滚须另行确认和验证。回退每日 5H 调度代码前先关闭该任务，避免旧调度逻辑重复触发。

验收：确认 tag 对应提交、CI 与 Security Scan 通过；Release 完成后再检查发布制品。部署后的界面、分组策略和每日 5H 业务验收参考本次 `docs/upstream-sync` 记录。

提交说明：`feat(release): ship account, model and gateway updates in v0.1.419`。
