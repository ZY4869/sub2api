# v0.1.279 更新说明

发布日期：2026-04-24

## 修复

- 修复 CI `backend-lint` 的 gofmt 格式检查失败：对新增/修改的测试文件应用 gofmt，确保 tag 构建可通过。

## 验证结果

- `go test ./...`（在 `backend` 目录执行）
- `npm run test:run`（在 `frontend` 目录执行）

