# v0.1.420 发布修复

日期：2026-09-09。包含 v0.1.419 全部改动，修复首次 tag 云端检查发现的问题。沿用用户对失败修复、递增版本和重新 Push 的授权。

## 失败证据与 Integration Map

- `v0.1.419` 提交 `a9be60695` 的 CI：https://github.com/ZY4869/sub2api/actions/runs/34304888442 。单元测试、后端构建、前端全量质量检查通过；Security Scan 也通过。
- Lint：普通/长上下文用量记录 → 管理员计费豁免 → 分组 Fast，第二步的实付成本被无条件覆盖，报两处 `ineffassign`。现在仅在用户未获豁免时应用分组政策，保留管理员免费金额及原因。增加两种记录入口 × 分组开关的业务结果回归。
- 集成：迁移 173/174 → 原整文件 SQL 迁移器 → groups 三列被各文件的 Down SQL 删除 → Ent 分组操作失败。保留已执行文件与 SHA256 校验和，新增正向补偿迁移 175；扩展真实数据库集成测试，检查默认 downgrade/false/false、写入 deny/true/true、非法值约束及重复启动迁移幂等性。
- 修正迁移 README 中与真实执行器不符的 Goose Up/Down 模板；不改变迁移器、公开 API 或现有生成代码。

## 验证与发布

- ✅ 本地用量记录和分组 Fast 相关回归通过，包含新增管理员免费计费回归。
- ✅ `go test -tags=integration -c` 集成测试编译通过。
- 🚫 本地 Docker 数据库集成未运行：Windows 未提供 Docker CLI，WSL 启动报 `HCS_E_SERVICE_NOT_AVAILABLE`。本地 Lint 继续执行；最终完整验证以 `v0.1.420` 对应 GitHub Actions 为准。
- 版本源、前端包版本和版本守卫同步为 `0.1.420`；仅追加提交和 tag，不覆盖 `v0.1.419`。
- 按用户要求，当 CI 与 Security Scan 通过且仅 Release 未完成时停止监测，不宣称制品发布已完成。

## 风险、回退与验收

本次不执行运行环境部署或数据库迁移。未来部署会应用补偿迁移 175，保留旧文件校验和及已有行；部署前应备份数据库。可回退到 `v0.1.418` 制品，但数据库回退和每日 5H 调度开关需单独处理，详见 `发布说明_v0.1.419.md`。

验收：新 tag 的 CI、Security Scan 通过；Release 完成后核对制品，后续部署验收分组创建、策略保存及管理员免费计费。失败日志保存在 `.cache/release-v0.1.419-20260909-103307/`。

提交说明：`fix(release): preserve billing exemptions and restore group policy columns`。
