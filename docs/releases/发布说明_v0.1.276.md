# v0.1.276 更新说明

发布日期：2026-04-23

## 修复

- 修复 `v0.1.275` Release/CI 因 Go lint（staticcheck QF1001）失败导致的 tag 编译失败：调整兼容生图归一逻辑中的条件判断，保持行为不变但通过 lint 校验。

## 验证结果

- `python scripts/verify_ci.py backend-lint`（在 `backend` 目录执行）
- `go test ./...`（在 `backend` 目录执行）
