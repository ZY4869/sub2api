# 超长代码拆分重构计划

## 计划说明
- 生成时间：2026-03-13
- 最后更新：2026-03-15
- 统计口径：以 `>= 395` **非空行**定义"超长文件"；排除 `node_modules`、`dist`、`bin`、`tmp`、`%TEMP%`、`migrations`；忽略 `frontend/src/router/index.ts`、`frontend/src/style.css`、`backend/cmd/server/wire_gen.go`（生成物豁免）；当前扫描共纳管 `324` 个（2026-03-15）。
- 目标：先治理真正影响迭代效率的手写业务文件，再滚动处理测试、国际化、生成物、大体量数据文件与锁文件治理项。
- 约束：所有拆分都要保持前后端契约不变；生成文件原则上不手改，而是回到上游 schema / generator / seed 来源治理。

## 当前进度
- 状态：第一波第 17 阶段已完成，账号管理前端已经依次完成“共享逻辑层拆分”“重复模板区块拆分”“模型限制/模型映射区块拆分”“配额控制/网关设置区块拆分”“Gemini 平台配置/共享生命周期开关拆分”“API Key 高重复高级设置拆分”“API Key 基础配置与平台默认值拆分”“Upstream 凭据区拆分”“运行时/分组设置区拆分”“CreateAccount 平台选择/账号类型/OAuth 前置分段拆分”“CreateAccount OAuth 授权区/底部操作区/帮助弹窗拆分”“BulkEdit 模型限制/自定义错误码区块拆分与冗余状态清理”“AccountsView 工具栏/弹窗协调层拆分与失效监听清理”“AccountsView 表格主体/行操作区拆分与父页表格渲染清理”“AccountsView 自动刷新/今日统计 composable 化与筛选补丁清理”“AccountsView 菜单状态/菜单定位/本地列表补丁下沉与失效页面逻辑清理”“BulkEdit 运行时/Anthropic/状态分组区块拆分与表单编排收敛”。
- 阶段 1 新增文件：`frontend/src/composables/useAccountTempUnschedRules.ts`、`frontend/src/composables/useAccountMixedChannelRisk.ts`、`frontend/src/utils/accountFormShared.ts`、`frontend/src/utils/__tests__/accountFormShared.spec.ts`。
- 阶段 1 完成内容：把 `CreateAccountModal.vue` / `EditAccountModal.vue` 里重复的 temp-unsched 规则编辑、mixed-channel 风险确认、pool retry 归一化提取为共享模块。
- 阶段 2 新增文件：`frontend/src/components/account/AccountTempUnschedRulesEditor.vue`、`frontend/src/components/account/AccountMixedChannelWarningDialog.vue`、`frontend/src/components/account/__tests__/AccountTempUnschedRulesEditor.spec.ts`。
- 阶段 2 完成内容：把 `CreateAccountModal.vue` / `EditAccountModal.vue` 里重复的 temp-unsched 编辑区和 mixed-channel 告警弹窗抽成独立子组件，并接回两个弹窗复用。
- 阶段 3 新增文件：`frontend/src/components/account/AccountModelRestrictionEditor.vue`、`frontend/src/components/account/AccountAntigravityModelMappingEditor.vue`、`frontend/src/components/account/__tests__/AccountModelRestrictionEditor.spec.ts`、`frontend/src/components/account/__tests__/AccountAntigravityModelMappingEditor.spec.ts`。
- 阶段 3 完成内容：把 `CreateAccountModal.vue` / `EditAccountModal.vue` 里重复的 whitelist/mapping 模型限制区、OpenAI OAuth 独立模型限制区、Antigravity 模型映射区抽成两个纯展示编辑器，并统一接回两个弹窗。
- 阶段 4 新增文件：`frontend/src/components/account/AccountQuotaControlEditor.vue`、`frontend/src/components/account/AccountGatewaySettingsEditor.vue`、`frontend/src/composables/useAnthropicQuotaControl.ts`、`frontend/src/utils/accountQuotaControl.ts`、`frontend/src/utils/__tests__/accountQuotaControl.spec.ts`、`frontend/src/components/account/__tests__/AccountQuotaControlEditor.spec.ts`、`frontend/src/components/account/__tests__/AccountGatewaySettingsEditor.spec.ts`。
- 阶段 4 完成内容：把 `CreateAccountModal.vue` / `EditAccountModal.vue` 里的 Anthropic 配额控制区抽成复用组件，把 OpenAI/Anthropic/Codex 网关开关区抽成复用组件，并把配额 extra 的读取/构建统一收敛到 composable + util，同时清理 `EditAccountModal.vue` 中失效的旧配额加载和手写 extra 拼装代码。
- 阶段 5 新增文件：`frontend/src/components/account/AccountGeminiAccountTypeEditor.vue`、`frontend/src/components/account/AccountAutoPauseToggle.vue`、`frontend/src/components/account/__tests__/AccountGeminiAccountTypeEditor.spec.ts`、`frontend/src/components/account/__tests__/AccountAutoPauseToggle.spec.ts`。
- 阶段 5 完成内容：把 `CreateAccountModal.vue` 里的 Gemini 账号类型/OAuth 类型/高级 OAuth 选项/等级选择区抽成独立组件，把 `CreateAccountModal.vue` / `EditAccountModal.vue` 共用的“到期自动暂停”开关抽成复用组件，并清理两个弹窗里残留的乱码注释与 Gemini 硬编码坏文案。
- 阶段 6 新增文件：`frontend/src/components/account/AccountPoolModeEditor.vue`、`frontend/src/components/account/AccountCustomErrorCodesEditor.vue`、`frontend/src/utils/accountApiKeyAdvancedSettings.ts`、`frontend/src/utils/__tests__/accountApiKeyAdvancedSettings.spec.ts`、`frontend/src/components/account/__tests__/AccountPoolModeEditor.spec.ts`、`frontend/src/components/account/__tests__/AccountCustomErrorCodesEditor.spec.ts`。
- 阶段 6 完成内容：把 `CreateAccountModal.vue` / `EditAccountModal.vue` 里重复的 Pool Mode 和自定义错误码编辑区抽成复用组件，并把相关状态默认值收敛到共享 util，同时删除两个弹窗里重复的错误码增删校验函数和失效残留代码。
- 阶段 7 新增文件：`frontend/src/components/account/AccountApiKeyBasicSettingsEditor.vue`、`frontend/src/utils/accountApiKeyBasicSettings.ts`、`frontend/src/components/account/__tests__/AccountApiKeyBasicSettingsEditor.spec.ts`、`frontend/src/utils/__tests__/accountApiKeyBasicSettings.spec.ts`。
- 阶段 7 完成内容：把 `CreateAccountModal.vue` / `EditAccountModal.vue` 里重复的 API Key 基础配置区（`base_url`、`api_key`、Gemini API Key tier、非 Antigravity 的模型限制编辑器）抽成复用组件，并把平台默认 `base_url`、占位符、提示文案 key 收敛到共享 util；同时清理 `CreateAccountModal.vue` / `EditAccountModal.vue` 里失效的旧平台默认值分支、遗留乱码注释，以及已经停用的 `wsModeCtxPool` 注释选项。
- 阶段 8 新增文件：`frontend/src/components/account/AccountUpstreamSettingsEditor.vue`、`frontend/src/utils/accountUpstreamSettings.ts`、`frontend/src/components/account/__tests__/AccountUpstreamSettingsEditor.spec.ts`、`frontend/src/utils/__tests__/accountUpstreamSettings.spec.ts`。
- 阶段 8 完成内容：把 `CreateAccountModal.vue` 里的 Antigravity upstream 凭据配置区与 `EditAccountModal.vue` 里的 upstream 凭据编辑区抽成复用组件，并把 upstream 的占位符和“创建必填 / 编辑保留旧值”提示 key 收敛到共享 util；同时清理两个弹窗里重复的 upstream 硬编码模板与失效的文案分散定义。
- 阶段 9 新增文件：`frontend/src/components/account/AccountRuntimeSettingsEditor.vue`、`frontend/src/components/account/AccountGroupSettingsEditor.vue`、`frontend/src/utils/accountRuntimeSettings.ts`、`frontend/src/components/account/__tests__/AccountRuntimeSettingsEditor.spec.ts`、`frontend/src/components/account/__tests__/AccountGroupSettingsEditor.spec.ts`、`frontend/src/utils/__tests__/accountRuntimeSettings.spec.ts`。
- 阶段 9 完成内容：把 `CreateAccountModal.vue` / `EditAccountModal.vue` 里重复的运行时设置区（代理、并发、负载因子、优先级、计费倍率、过期时间）和分组设置区（mixed scheduling + GroupSelector）抽成复用组件，并把并发/负载因子的归一化逻辑收敛到共享 util；同时删除两个弹窗里重复的 inline 归一化表达式、重复的 mixed scheduling tooltip 模板，以及 `EditAccountModal.vue` 里已经失效的只读 mixed scheduling 旧 UI 残留。
- 阶段 10 新增文件：`frontend/src/components/account/AccountCreatePlatformSelector.vue`、`frontend/src/components/account/AccountCreatePlatformTypeEditor.vue`、`frontend/src/components/account/AccountCreateTypeCardGroup.vue`、`frontend/src/components/account/AccountCreateAddMethodSelector.vue`、`frontend/src/components/account/__tests__/AccountCreatePlatformSelector.spec.ts`、`frontend/src/components/account/__tests__/AccountCreatePlatformTypeEditor.spec.ts`。
- 阶段 10 完成内容：把 `CreateAccountModal.vue` 顶部的平台选择、Sora/OpenAI/Anthropic/Antigravity 账号类型卡片、Anthropic `addMethod` 单选区与 Gemini/Antigravity 前置分段抽成独立组件，并复用已有的 `AccountGeminiAccountTypeEditor`、`AccountUpstreamSettingsEditor`；同时删除 `CreateAccountModal.vue` 中失效的手写平台 SVG 按钮模板、重复的账号类型卡片模板、重复的 Anthropic `addMethod` radios，以及已不再参与渲染的 `antigravityModelRestrictionMode` / `antigravityWhitelistModels` 状态与未使用的 `Icon` 引用。
- 阶段 11 新增文件：`frontend/src/components/account/AccountCreateOAuthStep.vue`、`frontend/src/components/account/AccountCreateFooterActions.vue`、`frontend/src/components/account/AccountGeminiHelpDialog.vue`、`frontend/src/components/account/oauthFlow.types.ts`、`frontend/src/components/account/__tests__/AccountCreateOAuthStep.spec.ts`、`frontend/src/components/account/__tests__/AccountCreateFooterActions.spec.ts`、`frontend/src/components/account/__tests__/AccountGeminiHelpDialog.spec.ts`。
- 阶段 11 完成内容：把 `CreateAccountModal.vue` 的 Step 2 OAuth 授权区、底部操作区与 Gemini 帮助弹窗抽成独立组件，并把 OAuth flow 的 exposed 类型收敛到共享 `oauthFlow.types.ts`，同时接回 `ReAuthAccountModal.vue` 复用；另外把 `AccountGeminiHelpDialog.vue` 内的静态链接/配额表改成数据驱动渲染，避免新拆出的帮助弹窗再次膨胀，并清理 `CreateAccountModal.vue` 中只服务于旧弹窗模板的链接常量与重复内联按钮模板。
- 阶段 12 新增文件：`frontend/src/components/account/BulkEditModelRestrictionSection.vue`、`frontend/src/components/account/BulkEditCustomErrorCodesSection.vue`、`frontend/src/components/account/__tests__/BulkEditModelRestrictionSection.spec.ts`、`frontend/src/components/account/__tests__/BulkEditCustomErrorCodesSection.spec.ts`。
- 阶段 12 完成内容：把 `BulkEditAccountModal.vue` 里的模型限制区和自定义错误码区抽成独立子组件，父弹窗只保留聚合状态与提交编排；同时删除失效的 `filteredModels` / `filteredPresets` 透传 computed、本地重复的 `ModelMapping` 接口，以及只服务旧内联模板的错误码与模型映射 helper，并把弹窗关闭时的大段状态重置收敛为 `resetFormState()`。
- 阶段 13 新增文件：`frontend/src/components/admin/account/AccountsViewToolbar.vue`、`frontend/src/components/admin/account/AccountsViewDialogsHost.vue`、`frontend/src/components/admin/account/__tests__/AccountsViewToolbar.spec.ts`、`frontend/src/components/admin/account/__tests__/AccountsViewDialogsHost.spec.ts`。
- 阶段 13 完成内容：把 `AccountsView.vue` 顶部的筛选/批量操作工具栏、自动刷新/列设置下拉和列表待同步提示抽成 `AccountsViewToolbar.vue`，并把底部一串创建/编辑/批量编辑/测试/统计/导入导出/错误透传等弹窗挂载统一收敛到 `AccountsViewDialogsHost.vue`；同时删除父页面里已经只服务旧模板的 `showAutoRefreshDropdown` / `showColumnDropdown` / `autoRefreshDropdownRef` / `columnDropdownRef` 状态、文档级点击关闭监听和相关 `Icon`/弹窗组件冗余导入，并修正 `TempUnschedStatusModal` 重置后必须走专门收尾逻辑的回归点。
- 阶段 14 新增文件：`frontend/src/components/admin/account/AccountsViewTable.vue`、`frontend/src/components/admin/account/AccountsViewRowActions.vue`、`frontend/src/components/admin/account/__tests__/AccountsViewTable.spec.ts`、`frontend/src/components/admin/account/__tests__/AccountsViewRowActions.spec.ts`。
- 阶段 14 完成内容：把 `AccountsView.vue` 中整段 `DataTable`/分页模板和行操作按钮区抽成 `AccountsViewTable.vue` 与 `AccountsViewRowActions.vue`，同时把过期时间/最近使用时间等表格专属展示格式化一起下沉到表格组件；父页面则删除不再使用的表格渲染导入、旧的 `Event` 型全选回调签名，以及只服务旧表格模板的过期时间 helper，并把 `openMenu` 重构成更清晰的 `handleOpenMenu` 事件入口。
- 阶段 15 新增文件：`frontend/src/composables/useAccountsViewLiveSync.ts`、`frontend/src/composables/useAccountsAutoRefresh.ts`、`frontend/src/composables/useAccountsTodayStats.ts`、`frontend/src/utils/accountListSync.ts`、`frontend/src/composables/__tests__/useAccountsViewLiveSync.spec.ts`、`frontend/src/utils/__tests__/accountListSync.spec.ts`。
- 阶段 15 完成内容：把 `AccountsView.vue` 里的 auto-refresh / ETag 增量同步、首屏轻量加载包装和今日统计刷新协调层下沉到 `useAccountsViewLiveSync.ts`，并继续拆成 `useAccountsAutoRefresh.ts` 与 `useAccountsTodayStats.ts` 两个更小的 composable；同时把本地列表补丁用到的筛选匹配、运行时字段合并和增量替换判断收敛到 `accountListSync.ts`，顺手清理掉旧逻辑里遗漏 `group` / `temp_unschedulable` 过滤、自动刷新增量请求未携带 `group` 条件，以及初始加载 Promise 未兜底的问题。
- 阶段 16 新增文件：`frontend/src/composables/useAccountActionMenu.ts`、`frontend/src/composables/useAccountsViewListPatching.ts`、`frontend/src/utils/accountActionMenuPosition.ts`、`frontend/src/composables/__tests__/useAccountsViewListPatching.spec.ts`、`frontend/src/utils/__tests__/accountActionMenuPosition.spec.ts`。
- 阶段 16 完成内容：把 `AccountsView.vue` 里的菜单状态/定位计算和本地列表补丁下沉到 `useAccountActionMenu.ts`、`accountActionMenuPosition.ts` 与 `useAccountsViewListPatching.ts`；同时让 `useAccountsViewLiveSync.ts` 只依赖 `isActionMenuOpen` / `syncAccountRefs` 这类页面级接口，并清理父页面中已经失效的 `legacyHandleOpenMenu`、`syncPaginationAfterLocalRemoval` 与相关乱码残留逻辑。
- 阶段 17 新增文件：`frontend/src/components/account/BulkEditAnthropicControlSection.vue`、`frontend/src/components/account/BulkEditRuntimeFieldsSection.vue`、`frontend/src/components/account/BulkEditStatusGroupSection.vue`、`frontend/src/composables/useBulkEditAccountForm.ts`、`frontend/src/utils/bulkEditAccountForm.ts`、`frontend/src/components/account/__tests__/BulkEditAnthropicControlSection.spec.ts`、`frontend/src/components/account/__tests__/BulkEditRuntimeFieldsSection.spec.ts`、`frontend/src/components/account/__tests__/BulkEditStatusGroupSection.spec.ts`、`frontend/src/utils/__tests__/bulkEditAccountForm.spec.ts`。
- 阶段 17 完成内容：把 `BulkEditAccountModal.vue` 中的 Anthropic 控制区、运行时字段区、状态/分组区继续拆成独立 section，并把批量编辑表单状态、payload 构建、预检条件判断下沉到 `useBulkEditAccountForm.ts` + `bulkEditAccountForm.ts`；同时让批量编辑接回共享 `useAccountMixedChannelRisk.ts`，删除失效的本地 mixed-channel 编排和过时的 `user_msg_queue_enabled` 兼容写法。
- 阶段 18 新增文件：`frontend/src/utils/accountApiKeyAdvancedSettingsForm.ts`、`frontend/src/utils/__tests__/accountApiKeyAdvancedSettingsForm.spec.ts`。
- 阶段 18 完成内容：合并并删除 3 个“微型 util”（`accountUpstreamSettings.ts`、`accountRuntimeSettings.ts`、`accountApiKeyAdvancedSettings.ts`）与对应测试，将有效逻辑收敛回 `accountFormShared.ts`；同时新增 `accountApiKeyAdvancedSettingsForm.ts` 统一封装 pool mode 与 custom error codes 在 Create/Edit 两端的 load/reset/apply 逻辑，删除 `CreateAccountModal.vue`/`EditAccountModal.vue` 内重复分支实现。
- 当前结果：`CreateAccountModal.vue` 已从 `4383` 行降到 `1805` 行，`EditAccountModal.vue` 已从 `2496` 行降到 `878` 行，`AccountsView.vue` 已从 `1264` 行降到 `761` 行，`BulkEditAccountModal.vue` 已从 `1246` 行降到 `334` 行并退出超长文件清单；`BulkEditAnthropicControlSection.vue` 为 `188` 行，`BulkEditRuntimeFieldsSection.vue` 为 `175` 行，`BulkEditStatusGroupSection.vue` 为 `71` 行，`useBulkEditAccountForm.ts` 为 `89` 行，`bulkEditAccountForm.ts` 为 `191` 行；`AccountCreateOAuthStep.vue` 为 `74` 行，`AccountCreateFooterActions.vue` 为 `123` 行，`AccountGeminiHelpDialog.vue` 为 `189` 行，`BulkEditModelRestrictionSection.vue` 为 `277` 行，`BulkEditCustomErrorCodesSection.vue` 为 `178` 行，`AccountsViewToolbar.vue` 为 `248` 行，`AccountsViewDialogsHost.vue` 为 `218` 行，`AccountsViewTable.vue` 为 `251` 行，`AccountsViewRowActions.vue` 为 `73` 行，`useAccountsViewLiveSync.ts` 为 `227` 行，`useAccountsAutoRefresh.ts` 为 `131` 行，`useAccountsTodayStats.ts` 为 `64` 行，`accountListSync.ts` 为 `84` 行，`useAccountActionMenu.ts` 为 `53` 行，`useAccountsViewListPatching.ts` 为 `66` 行，`accountActionMenuPosition.ts` 为 `58` 行。
- 当前验证：已通过 `pnpm test:run src/components/account/__tests__/BulkEditAccountModal.spec.ts src/components/account/__tests__/BulkEditCustomErrorCodesSection.spec.ts src/components/account/__tests__/BulkEditModelRestrictionSection.spec.ts src/components/account/__tests__/BulkEditRuntimeFieldsSection.spec.ts src/components/account/__tests__/BulkEditAnthropicControlSection.spec.ts src/components/account/__tests__/BulkEditStatusGroupSection.spec.ts src/utils/__tests__/bulkEditAccountForm.spec.ts`、`pnpm typecheck`、`pnpm exec eslint src/components/account/BulkEditAccountModal.vue src/components/account/BulkEditAnthropicControlSection.vue src/components/account/BulkEditRuntimeFieldsSection.vue src/components/account/BulkEditStatusGroupSection.vue src/components/account/index.ts src/components/account/__tests__/BulkEditAccountModal.spec.ts src/components/account/__tests__/BulkEditAnthropicControlSection.spec.ts src/components/account/__tests__/BulkEditRuntimeFieldsSection.spec.ts src/components/account/__tests__/BulkEditStatusGroupSection.spec.ts src/components/account/__tests__/BulkEditCustomErrorCodesSection.spec.ts src/components/account/__tests__/BulkEditModelRestrictionSection.spec.ts src/composables/useBulkEditAccountForm.ts src/utils/bulkEditAccountForm.ts src/utils/__tests__/bulkEditAccountForm.spec.ts`、`git diff --check`（仅剩既有 CRLF warning）。
- 下一阶段：继续第一波，优先把 `BulkEditAccountModal.vue` 剩余的 Base URL/提交结果编排再下沉一层，并评估是否切回 `CreateAccountModal.vue` 继续压缩聚合入口。

## 优先级定义
| 优先级 | 含义 | 建议节奏 |
| --- | --- | --- |
| P0 | 立即拆。核心手写业务文件，已经明显阻塞理解、联调和发版。 | 本周到下个迭代优先完成。 |
| P1 | 高优先。仍是手写核心文件，但可以在 P0 后按模块并行推进。 | 1 到 2 个迭代内完成。 |
| P2 | 滚动治理。中等风险长文件，适合结合需求顺手拆。 | 日常迭代中持续清理。 |
| P3 | 治理型任务。主要是生成物、文档、超长测试、大数据文件和锁文件。 | 以“治理上游来源”为主，不建议直接手拆生成产物。 |

## 总览
- P0：12 个文件
- P1：28 个文件
- P2：126 个文件
- P3：158 个文件
- 执行顺序建议：**先后端 P0/P1**（账号/计费/限流/调度/鉴权 + ops 聚合），再处理中等风险长文件（P2），最后集中做测试/i18n/生成物/数据治理（P3）。
- **微型文件合并待办**：已完成（逻辑已收敛至 `accountFormShared.ts`，并删除原微型文件与测试）。

## 统一拆分守则
- 单文件目标：控制在 `150-200` 行附近；复杂页面、服务和仓储允许存在聚合入口，但入口不应再承载细节实现。
- 单函数 / 单 Hook 目标：控制在 `30` 行附近；超出后优先抽纯函数、子状态机或 DTO / presenter mapper。
- 前端拆分模板：`PageShell` / `Filters` / `TableOrList` / `Dialogs` / `useXxxPage` / `types` / `api`。
- 后端拆分模板：`facade/orchestrator` / `validator` / `adapter` / `calculator` / `mapper` / `query` / `presenter`。
- 生成物治理：`backend/ent/*`、`frontend/src/generated/*`、大型 seed / registry 文件都要回到 schema、generator 或原始数据切片层处理。
- 每波拆分都要补最小验证：前端 `typecheck + 关键组件测试`，后端 `go test` 至少覆盖被拆模块主链路。
- **拆分豁免**：以下类型文件不适用 150-200 行阈值，不纳入拆分清单：
  - **生成物**：`wire_gen.go`、`backend/ent/*`、`frontend/src/generated/*` 等自动生成文件，不手改。
  - **已落地的 SQL migration**：已执行的迁移脚本拆分既危险又无意义，仅对未来新迁移做预防。
  - **声明式配置文件**：路由声明（`router/index.ts`）、全局样式（`style.css`）、示例配置（`config.example.yaml`）等天然扁平声明式，拆分增加导航成本但不降低认知复杂度。
  - **纯文档**：README、VISUAL_GUIDE.md、USAGE_EXAMPLES.md 等拆分会降低可读性和可发现性。
  - **部署脚本**：`install.sh` 等追求单文件可移植执行，拆分引入 source 复杂度。
  - **JSON 数据文件**：`preset_overlay.json` 等归 P3 上游数据治理，不做手工拆分。
  - **依赖锁文件**：`pnpm-lock.yaml` 永远不该手动拆。
  - **测试文件**：测试天然冗长（fixture + 断言），除非单文件 >2000 行，否则不强制拆分。
- **防止过度碎片化**：拆出的文件应有足够的独立语义；<50 行的微型组件若只有一个消费者、<30 行的微型 util 应优先合并到已有共享模块，避免文件爆炸。

## 执行波次建议
1. 第一波：账号管理前端（已完成至第 17 阶段）。
2. 第二波：网关与流式后端（已完成，见下方第二波阶段表）。
3. 第三波（优先后端）：账号/计费/限流/调度/鉴权。优先拆 `account_test_service.go`、`account.go`、`ratelimit_service.go`、`crs_sync_service.go`、`ops_repo.go`，并把高频策略/计算逻辑抽纯函数补单测。
4. 第四波（后置项也要做）：测试拆分 + 国际化拆分 + 生成物/seed/data 分片治理。目标：拆掉 >2000 行测试；把 `i18n/locales/*/admin.ts` 按 feature 分片；把 `generated/modelRegistry.ts` 与 registry/overlay JSON 走"分片 + 合并"脚本/生成器。
5. 第五波：管理台页面与组件（Groups/Settings/Proxies/Users/Subscriptions/PromoCodes/Ops 等），结合需求滚动拆。
6. 第六波：中优先级 Service/Repository/Handler/Composable/Store/API，持续清理（防止回弹）。

### 第二波（网关与流式后端）— 2026-03-15 拆分蓝图（>=800 行聚焦）

#### 拆分边界（必须保持）
- 仅做“文件拆分 / 提取纯函数 / 目录归位”，不改业务行为、不改接口、不改 DB schema。
- 保持路由、请求/响应结构、错误码不变；拆分只做内部组织调整。
- 日志/审计/指标保持不变：继续透传 `requestId/correlationId`，不重命名既有 metrics/event 名称。
- 每阶段都要有最小验证（`go test`），并把验证命令与结果记录在本节的阶段表内。

#### 拆分前基线行数（2026-03-15 扫描，>=800 行聚焦）
> 说明：基线行数以二进制 LF 计数（用于跨平台一致对比）。与旧表格不一致时，以本节为准。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/service/gateway_account_selection.go` | 1858 |
| `backend/internal/service/openai_ws_pool.go` | 1713 |
| `backend/internal/service/openai_gateway_response.go` | 1653 |
| `backend/internal/service/gateway_response_stream.go` | 1513 |
| `backend/internal/handler/openai_gateway_handler.go` | 1568 |
| `backend/internal/service/sora_gateway_service.go` | 1553 |

#### 第二波阶段表（第二波-阶段 1..6）
| 阶段 | 目标文件 | 拆分产物（新文件名） | 验证命令（在 `backend/` 下） | 回归关注点 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 第二波-阶段 1 | `backend/internal/handler/openai_gateway_handler.go` | `openai_gateway_handler_responses.go`<br/>`openai_gateway_handler_websocket.go`<br/>`openai_gateway_handler_messages.go`<br/>`openai_gateway_handler_deps.go`<br/>`openai_gateway_handler_errors.go`<br/>`openai_gateway_handler_recover.go`<br/>`openai_gateway_handler_usage.go` | `go test ./internal/handler -run TestOpenAI -count=1` | `/openai/v1/responses`（HTTP+WS）在“已开始流式/未开始流式”两种情况下错误回写一致；panic recover 不吞错、不覆盖已写响应 | ✅ 通过（2026-03-15） |
| 第二波-阶段 2 | `backend/internal/service/openai_ws_pool.go` | `openai_ws_types.go`<br/>`openai_ws_lease.go`<br/>`openai_ws_conn.go`<br/>`openai_ws_pool_background.go`<br/>`openai_ws_pool_metrics.go`<br/>`openai_ws_pool_limits.go`<br/>`openai_ws_pool_helpers.go` | `go test ./internal/service -run TestOpenAIWS -count=1` | Acquire 队列/复用/ForceNewConn/ForcePreferredConn 行为不变；后台 worker 关闭与资源回收不泄漏（现有测试覆盖） | ✅ 通过（2026-03-15） |
| 第二波-阶段 3 | `backend/internal/service/openai_gateway_response.go` | `openai_gateway_forward.go`<br/>`openai_gateway_passthrough.go`<br/>`openai_gateway_headers.go`<br/>`openai_gateway_upstream_request.go`<br/>`openai_gateway_streaming.go`<br/>`openai_gateway_nonstreaming.go`<br/>`openai_gateway_compact.go` | `go test ./internal/service -run TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient -count=1` | passthrough header 透传与 timeout header 过滤策略不变；streaming/non-streaming usage 提取、模型名替换行为一致；compact 相关测试不破 | ✅ 通过（2026-03-15） |
| 第二波-阶段 4 | `backend/internal/service/gateway_response_stream.go` | `gateway_forward.go`<br/>`gateway_passthrough_anthropic_apikey.go`<br/>`gateway_response_error.go`<br/>`gateway_response_streaming.go`<br/>`gateway_response_nonstreaming.go` | `go test ./internal/service -run TestGatewayService_ -count=1` | mimic ClaudeCode 注入/normalize 逻辑保持一致；retry exhausted/failover side effects 时机不变 | ✅ 通过（2026-03-15） |
| 第二波-阶段 5 | `backend/internal/service/gateway_account_selection.go` | `gateway_account_selection_entry.go`<br/>`gateway_account_selection_group.go`<br/>`gateway_account_selection_candidates.go`<br/>`gateway_account_selection_prefetch.go`<br/>`gateway_account_selection_sort.go`<br/>`gateway_account_selection_platform.go`<br/>`gateway_account_selection_failure.go`<br/>`gateway_account_selection_model_support.go` | `go test -tags unit ./internal/service -run TestGatewayService_SelectAccount -count=1`<br/>`go test -tags unit ./internal/service -run TestSortAccountsByPriorityAndLastUsed -count=1` | sticky session/force platform/mixed scheduling/fallback 决策分支不变；失败诊断日志字段不变 | ✅ 通过（2026-03-15） |
| 第二波-阶段 6 | `backend/internal/service/sora_gateway_service.go` | `sora_gateway_options_parse.go`<br/>`sora_gateway_input_prompt.go`<br/>`sora_gateway_input_media.go`<br/>`sora_gateway_security.go`<br/>`sora_gateway_poll.go`<br/>`sora_gateway_stream.go`<br/>`sora_gateway_watermark_character.go` | `go test -tags unit ./internal/service -run TestSoraGatewayService_ -count=1`<br/>`go test -tags unit ./internal/service -run TestDecodeSoraImageInput_ -count=1` | SSRF/私网拦截逻辑不变；stream JSON 转义与 ping 行为不变；watermark/character/cameo 分支仍可通过现有单测 | ✅ 通过（2026-03-15） |

#### 第二波统一验收（2026-03-15）
- 验证命令（在 `backend/` 下）：`go test ./... -count=1`
- 结果：✅ 通过

## 当前 P0 文件（后端优先，按非空行计数）
| 文件 | 行数 |
| --- | ---: |
| `backend/internal/service/account_test_service.go` | 1618 |
| `backend/internal/service/account.go` | 1375 |
| `backend/internal/service/ratelimit_service.go` | 1305 |
| `backend/internal/service/crs_sync_service.go` | 1277 |
| `backend/internal/repository/ops_repo.go` | 1401 |
| `frontend/src/views/admin/ops/components/OpsDashboardHeader.vue` | 1473 |
| `frontend/src/views/admin/GroupsView.vue` | 2446 |
| `frontend/src/views/admin/SettingsView.vue` | 2395 |
| `frontend/src/views/admin/ProxiesView.vue` | 1721 |
| `frontend/src/views/user/KeysView.vue` | 1672 |
| `frontend/src/components/account/CreateAccountModal.vue` | 1805 |
| `frontend/src/types/index.ts` | 1406 |

## 当前超长文件全量清单（2026-03-15 扫描，后端优先）

> 口径：以 `>= 395` **非空行**定义“超长文件”；排除 `node_modules`、`dist`、`bin`、`tmp`、`%TEMP%`、`migrations` 等目录；并忽略 `frontend/src/router/index.ts`、`frontend/src/style.css`、`backend/cmd/server/wire_gen.go`（生成物豁免）。

- 总计：324 个
- P0：12 个
- P1：28 个
- P2：126 个
- P3：158 个

### P0（12 个）

#### Backend Service（4 个）
建议拆分方式：按 `facade/orchestrator`/`validator`/`calculator`/`adapter`/`mapper`/`presenter` 分层；金额/配额/策略类逻辑优先抽纯函数并补单测。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/service/account_test_service.go` | 1618 |
| `backend/internal/service/account.go` | 1375 |
| `backend/internal/service/ratelimit_service.go` | 1305 |
| `backend/internal/service/crs_sync_service.go` | 1277 |

#### Backend Repository（1 个）
建议拆分方式：拆成 query builder、row mapper、aggregation reader、cache facade，避免一个 repo 混杂写入、聚合与报表解释。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/repository/ops_repo.go` | 1401 |

#### Frontend 管理台 Ops 页面（1 个）
建议拆分方式：拆成 dashboard shell、指标卡/图表区、过滤状态、自动刷新与告警交互层；请求状态收敛到 `useXxx` composable。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/views/admin/ops/components/OpsDashboardHeader.vue` | 1473 |

#### Frontend 管理台页面（3 个）
建议拆分方式：拆成页面壳、筛选栏、表格/卡片区、弹窗协调层、`useXxxPage`；列定义与批量操作下沉。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/views/admin/GroupsView.vue` | 2446 |
| `frontend/src/views/admin/SettingsView.vue` | 2395 |
| `frontend/src/views/admin/ProxiesView.vue` | 1721 |

#### Frontend 用户页面（1 个）
建议拆分方式：拆成容器/过滤区/列表/详情抽屉/动作 hooks，页面只做编排。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/views/user/KeysView.vue` | 1672 |

#### Frontend 账号组件与弹窗（1 个）
建议拆分方式：优先拆容器组件、表单 schema、平台专属分区、提交适配器、纯展示子组件；把 `watch` 逻辑下沉到 composable。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/components/account/CreateAccountModal.vue` | 1805 |

#### Frontend 类型定义（1 个）
建议拆分方式：按领域拆 `account/usage/ops/system/shared` 等，避免全局类型堆叠。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/types/index.ts` | 1406 |

### P1（28 个）

#### Backend Service（13 个）
建议拆分方式：按 `facade/orchestrator`/`validator`/`calculator`/`adapter`/`mapper`/`presenter` 分层；金额/配额/策略类逻辑优先抽纯函数并补单测。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/service/auth_service.go` | 1108 |
| `backend/internal/service/openai_ws_forward_loop.go` | 1090 |
| `backend/internal/service/gemini_oauth_service.go` | 979 |
| `backend/internal/service/account_usage_service.go` | 977 |
| `backend/internal/service/sora_sdk_client.go` | 959 |
| `backend/internal/service/gemini_messages_forward.go` | 932 |
| `backend/internal/service/subscription_service.go` | 930 |
| `backend/internal/service/ops_alert_evaluator_service.go` | 894 |
| `backend/internal/service/antigravity_stream.go` | 864 |
| `backend/internal/service/openai_account_scheduler.go` | 839 |
| `backend/internal/service/ops_metrics_collector.go` | 826 |
| `backend/internal/service/billing_service.go` | 823 |
| `backend/internal/service/pricing_service.go` | 803 |

#### Backend Repository（3 个）
建议拆分方式：拆成 query builder、row mapper、aggregation reader、cache facade，避免一个 repo 混杂写入、聚合与报表解释。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/repository/usage_log_repo_aggregate.go` | 1084 |
| `backend/internal/repository/ops_repo_dashboard.go` | 930 |
| `backend/internal/repository/http_upstream.go` | 819 |

#### Backend Admin Handler（1 个）
建议拆分方式：按资源路由拆文件，分离 bind/validate、authz、service call、response presenter。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/handler/admin/ops_handler.go` | 828 |

#### Backend Handler（2 个）
建议拆分方式：按协议适配/鉴权/错误映射/响应写回拆；流式与非流式逻辑分文件。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/handler/ops_error_logger.go` | 1086 |
| `backend/internal/handler/sora_client_handler.go` | 868 |

#### Frontend 管理台页面（2 个）
建议拆分方式：拆成页面壳、筛选栏、表格/卡片区、弹窗协调层、`useXxxPage`；列定义与批量操作下沉。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/views/admin/SubscriptionsView.vue` | 1184 |
| `frontend/src/views/admin/UsersView.vue` | 1110 |

#### Frontend 管理台子组件（1 个）

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/components/admin/account/ScheduledTestsPanel.vue` | 831 |

#### Frontend 用户页面（1 个）
建议拆分方式：拆成容器/过滤区/列表/详情抽屉/动作 hooks，页面只做编排。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/views/user/UsageView.vue` | 826 |

#### Frontend 业务页面（1 个）

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/views/KeyUsageView.vue` | 818 |

#### Frontend 账号组件与弹窗（2 个）
建议拆分方式：优先拆容器组件、表单 schema、平台专属分区、提交适配器、纯展示子组件；把 `watch` 逻辑下沉到 composable。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/components/account/OAuthAuthorizationFlow.vue` | 933 |
| `frontend/src/components/account/EditAccountModal.vue` | 878 |

#### Frontend 管理台国际化（2 个）
建议拆分方式：按 feature 拆成多文件并在 `i18n` 层聚合导入；禁止继续在单文件追加。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/i18n/locales/zh/admin.ts` | 3766 |
| `frontend/src/i18n/locales/en/admin.ts` | 3616 |

### P2（126 个）

#### Backend Service（44 个）
建议拆分方式：按 `facade/orchestrator`/`validator`/`calculator`/`adapter`/`mapper`/`presenter` 分层；金额/配额/策略类逻辑优先抽纯函数并补单测。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/service/scheduler_snapshot_service.go` | 781 |
| `backend/internal/service/billing_cache_service.go` | 773 |
| `backend/internal/service/api_key_service.go` | 762 |
| `backend/internal/service/openai_ws_v2/passthrough_relay.go` | 756 |
| `backend/internal/service/gateway_request.go` | 745 |
| `backend/internal/service/openai_ws_pool.go` | 733 |
| `backend/internal/service/antigravity_retry.go` | 681 |
| `backend/internal/service/ops_service.go` | 651 |
| `backend/internal/service/gateway_claude_normalize.go` | 647 |
| `backend/internal/service/ops_scheduled_report_service.go` | 641 |
| `backend/internal/service/ops_retry.go` | 637 |
| `backend/internal/service/setting_service_public.go` | 590 |
| `backend/internal/service/gemini_messages_response.go` | 580 |
| `backend/internal/service/antigravity_upstream.go` | 563 |
| `backend/internal/service/gateway_response_streaming.go` | 509 |
| `backend/internal/service/model_registry_availability.go` | 508 |
| `backend/internal/service/ops_settings.go` | 505 |
| `backend/internal/service/sora_gateway_streaming_legacy.go` | 491 |
| `backend/internal/service/openai_gateway_messages.go` | 485 |
| `backend/internal/service/openai_oauth_service.go` | 475 |
| `backend/internal/service/gateway_passthrough_anthropic_apikey.go` | 468 |
| `backend/internal/service/email_service.go` | 466 |
| `backend/internal/service/update_service.go` | 466 |
| `backend/internal/service/sora_models.go` | 464 |
| `backend/internal/service/openai_gateway_client_restriction.go` | 461 |
| `backend/internal/service/openai_gateway_chat_completions.go` | 459 |
| `backend/internal/service/usage_record_worker_pool.go` | 456 |
| `backend/internal/service/openai_gateway_forward.go` | 455 |
| `backend/internal/service/model_catalog_sources.go` | 448 |
| `backend/internal/service/admin_service_groups.go` | 437 |
| `backend/internal/service/idempotency.go` | 433 |
| `backend/internal/service/openai_ws_replay.go` | 433 |
| `backend/internal/service/totp_service.go` | 430 |
| `backend/internal/service/gateway_account_selection_entry.go` | 429 |
| `backend/internal/service/openai_gateway_account_selection.go` | 427 |
| `backend/internal/service/setting_service_sora_s3.go` | 426 |
| `backend/internal/service/openai_codex_transform.go` | 419 |
| `backend/internal/service/openai_ws_metrics.go` | 419 |
| `backend/internal/service/wire.go` | 414 |
| `backend/internal/service/redeem_service.go` | 411 |
| `backend/internal/service/gateway_account_selection_platform.go` | 408 |
| `backend/internal/service/openai_ws_payload.go` | 408 |
| `backend/internal/service/gemini_quota.go` | 397 |
| `backend/internal/service/account_model_import_probe.go` | 395 |

#### Backend Repository（10 个）
建议拆分方式：拆成 query builder、row mapper、aggregation reader、cache facade，避免一个 repo 混杂写入、聚合与报表解释。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/repository/ops_repo_alerts.go` | 792 |
| `backend/internal/repository/api_key_repo.go` | 608 |
| `backend/internal/repository/group_repo.go` | 599 |
| `backend/internal/repository/ops_repo_trends.go` | 548 |
| `backend/internal/repository/usage_cleanup_repo.go` | 528 |
| `backend/internal/repository/user_repo.go` | 526 |
| `backend/internal/repository/concurrency_cache.go` | 491 |
| `backend/internal/repository/account_repo.go` | 436 |
| `backend/internal/repository/dashboard_aggregation_repo.go` | 429 |
| `backend/internal/repository/user_subscription_repo.go` | 425 |

#### Backend Admin Handler（8 个）
建议拆分方式：按资源路由拆文件，分离 bind/validate、authz、service call、response presenter。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/handler/admin/ops_ws_handler.go` | 665 |
| `backend/internal/handler/admin/account_data.go` | 558 |
| `backend/internal/handler/admin/ops_alerts_handler.go` | 549 |
| `backend/internal/handler/admin/usage_handler.go` | 531 |
| `backend/internal/handler/admin/account_handler_runtime.go` | 519 |
| `backend/internal/handler/admin/dashboard_handler.go` | 495 |
| `backend/internal/handler/admin/data_management_handler.go` | 489 |
| `backend/internal/handler/admin/setting_handler.go` | 450 |

#### Backend Handler（8 个）
建议拆分方式：按协议适配/鉴权/错误映射/响应写回拆；流式与非流式逻辑分文件。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/handler/dto/mappers.go` | 674 |
| `backend/internal/handler/gemini_v1beta_handler.go` | 659 |
| `backend/internal/handler/auth_linuxdo_oauth.go` | 652 |
| `backend/internal/handler/sora_gateway_handler.go` | 638 |
| `backend/internal/handler/auth_handler.go` | 509 |
| `backend/internal/handler/openai_gateway_handler_responses.go` | 496 |
| `backend/internal/handler/gateway_handler_messages.go` | 469 |
| `backend/internal/handler/dto/types.go` | 423 |

#### Backend pkg（8 个）
建议拆分方式：按“协议/转换器/状态机/IO”分离；避免 pkg 内部继续堆业务分支。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/pkg/antigravity/request_transformer.go` | 668 |
| `backend/internal/pkg/antigravity/client.go` | 503 |
| `backend/internal/pkg/logger/logger.go` | 489 |
| `backend/internal/pkg/tlsfingerprint/dialer.go` | 489 |
| `backend/internal/pkg/antigravity/schema_cleaner.go` | 482 |
| `backend/internal/pkg/apicompat/responses_to_anthropic.go` | 455 |
| `backend/internal/pkg/antigravity/stream_transformer.go` | 431 |
| `backend/internal/pkg/apicompat/types.go` | 399 |

#### Backend 配置与校验（1 个）
建议拆分方式：校验规则表驱动化；把默认值/校验/错误消息拆开，避免一个文件混杂读取 + 校验 + 文案。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/config/config_validate.go` | 732 |

#### Backend Server / Routes（1 个）
建议拆分方式：路由声明保持薄层；把 handler 组装与中间件配置拆到独立文件。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/server/routes/admin.go` | 484 |

#### Backend Setup（1 个）
建议拆分方式：把 wiring、初始化、健康检查、启动校验拆开。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/setup/setup.go` | 559 |

#### Frontend 管理台 Ops 页面（8 个）
建议拆分方式：拆成 dashboard shell、指标卡/图表区、过滤状态、自动刷新与告警交互层；请求状态收敛到 `useXxx` composable。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/views/admin/ops/OpsDashboard.vue` | 723 |
| `frontend/src/views/admin/ops/components/OpsAlertEventsCard.vue` | 592 |
| `frontend/src/views/admin/ops/components/OpsConcurrencyCard.vue` | 559 |
| `frontend/src/views/admin/ops/components/OpsAlertRulesCard.vue` | 538 |
| `frontend/src/views/admin/ops/components/OpsSettingsDialog.vue` | 528 |
| `frontend/src/views/admin/ops/components/OpsRuntimeSettingsCard.vue` | 484 |
| `frontend/src/views/admin/ops/components/OpsSystemLogTable.vue` | 468 |
| `frontend/src/views/admin/ops/components/OpsEmailNotificationCard.vue` | 404 |

#### Frontend 管理台页面（7 个）
建议拆分方式：拆成页面壳、筛选栏、表格/卡片区、弹窗协调层、`useXxxPage`；列定义与批量操作下沉。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/views/admin/AccountsView.vue` | 731 |
| `frontend/src/views/admin/RedeemView.vue` | 704 |
| `frontend/src/views/admin/PromoCodesView.vue` | 663 |
| `frontend/src/views/admin/models/ModelRegistryView.vue` | 636 |
| `frontend/src/views/admin/DashboardView.vue` | 557 |
| `frontend/src/views/admin/AnnouncementsView.vue` | 494 |
| `frontend/src/views/admin/DataManagementView.vue` | 481 |

#### Frontend 管理台子组件（5 个）

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/components/admin/account/AccountStatsModal.vue` | 666 |
| `frontend/src/components/admin/ErrorPassthroughRulesModal.vue` | 607 |
| `frontend/src/components/admin/account/ReAuthAccountModal.vue` | 495 |
| `frontend/src/components/admin/account/AccountTestModal.vue` | 467 |
| `frontend/src/components/admin/usage/UsageFilters.vue` | 409 |

#### Frontend 用户页面（1 个）
建议拆分方式：拆成容器/过滤区/列表/详情抽屉/动作 hooks，页面只做编排。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/views/user/RedeemView.vue` | 469 |

#### Frontend 业务页面（4 个）

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/views/auth/RegisterView.vue` | 671 |
| `frontend/src/views/setup/SetupWizardView.vue` | 625 |
| `frontend/src/views/HomeView.vue` | 598 |
| `frontend/src/views/auth/EmailVerifyView.vue` | 400 |

#### Frontend 账号组件与弹窗（2 个）
建议拆分方式：优先拆容器组件、表单 schema、平台专属分区、提交适配器、纯展示子组件；把 `watch` 逻辑下沉到 composable。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/components/account/AccountStatsModal.vue` | 702 |
| `frontend/src/components/account/ReAuthAccountModal.vue` | 485 |

#### Frontend 通用组件（4 个）

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/components/common/DataTable.vue` | 654 |
| `frontend/src/components/common/AnnouncementBell.vue` | 520 |
| `frontend/src/components/common/VersionBadge.vue` | 511 |
| `frontend/src/components/common/Select.vue` | 456 |

#### Frontend 组件（5 个）

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/components/layout/AppSidebar.vue` | 664 |
| `frontend/src/components/sora/SoraPromptBar.vue` | 654 |
| `frontend/src/components/keys/UseKeyModal.vue` | 652 |
| `frontend/src/components/sora/SoraProgressCard.vue` | 532 |
| `frontend/src/components/sora/SoraLibraryPage.vue` | 507 |

#### Frontend API（3 个）

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/api/admin/ops/types.ts` | 690 |
| `frontend/src/api/admin/accounts.ts` | 654 |
| `frontend/src/api/admin/settings.ts` | 483 |

#### Frontend Composable（2 个）

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/composables/useAccountUsagePresentation.ts` | 672 |
| `frontend/src/composables/useOnboardingTour.ts` | 480 |

#### Frontend Store（1 个）

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/stores/app.ts` | 402 |

#### Backend 测试（3 个）
建议拆分方式：按场景/协议拆；复用 fixture builder，避免单文件“巨石测试脚本”。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/service/gateway_multiplatform_test.go` | 2871 |
| `backend/internal/handler/sora_client_handler_test.go` | 2772 |
| `backend/internal/service/openai_ws_forwarder_ingress_session_test.go` | 2338 |

### P3（158 个）

#### Backend 测试（65 个）
建议拆分方式：按场景/协议拆；复用 fixture builder，避免单文件“巨石测试脚本”。

> 本组为 <2000 行的后端测试（>2000 行的测试已归入 P2）。行数范围 399 - 1628。建议后续按领域/协议拆分并统一 fixture。

获取完整列表命令（在 `backend/` 下）：`go test ./... -count=1`（回归） + `find internal -name "*_test.go" -maxdepth 5 | xargs wc -l | sort -rn`（大小排序）

#### Frontend 测试（1 个）
建议拆分方式：按页面/用例拆分；公共 mock/fixture 抽到 helper。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/components/account/__tests__/AccountUsageCell.spec.ts` | 436 |

#### Backend Ent 生成代码（88 个）
治理方式：不手改生成物；通过拆 schema/mixin/hook/template 与限制 schema 复杂度来间接控体积。

> 共 88 个文件（`backend/ent/` 目录），行数范围 414 - 24965。最大文件为 `backend/ent/mutation.go`(24965行)、`backend/ent/client.go`(3462行)、`backend/ent/group_create.go`(2790行)。

#### Frontend 生成代码（1 个）
治理方式：不手改生成物；改 generator，按 provider/模块分片输出并支持懒加载。

| 文件 | 行数 |
| --- | ---: |
| `frontend/src/generated/modelRegistry.ts` | 7120 |

#### 大型数据文件与种子（JSON）（3 个）
治理方式：按 provider/模型族/日期分片 + 索引清单；提供构建合并脚本，避免单文件继续膨胀。

| 文件 | 行数 |
| --- | ---: |
| `backend/internal/modelregistry/registry_seed.json` | 6673 |
| `backend/resources/model-pricing/model_prices_and_context_window.json` | 4734 |
| `backend/internal/modelregistry/preset_overlay.json` | 410 |
