# Third-Party License Audit Notes

This file records targeted dependency checks for integrations added outside the
regular lockfile review.

| Package | Version | Scope | License | Verification |
| --- | --- | --- | --- | --- |
| `@airwallex/components-sdk` | `1.32.0` | Frontend Airwallex embedded payment elements | MIT | npm/jsDelivr package metadata shows `License: MIT` for the package line; version is pinned in `frontend/package.json` and `frontend/pnpm-lock.yaml`. |

The repository root `LICENSE` remains MIT.
