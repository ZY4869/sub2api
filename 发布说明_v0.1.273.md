# v0.1.273 更新说明

发布日期：2026-04-23

## 重点更新

- 完成 OpenAI 生图 / 编辑图双协议收尾：native、compat 与 `/v1/responses` image tool 统一共享同一套图片能力矩阵，不再出现行为口径漂移。
- GPT image profile 统一放开 `stream`、多图、`mask`、`background=transparent` 与最大边 `3840px` 的自定义尺寸；未知或旧模型继续走保守拒绝路径。
- 管理后台账号 / 分组相关设置与用量展示链路继续补强，账户列表、创建 / 编辑弹窗、用量进度条与相关工具栏交互同步更新。

## 后端能力补充

- 固化 OpenAI 图片能力解析器，统一 native / compat / responses image_generation tool 的前置校验与 `image_capability_profile` 观测口径。
- 补齐 `groups.image_protocol_mode` 的 migration / schema 回归，确保默认值与存量升级行为可验证。
- 扩展 request metadata、ops trace 与 runtime metrics 断言，统一切换到 GPT image profile 的 `transparent_on` 档位。

## 前端联动

- 管理后台账号页补强 OpenAI Protocol Gateway 配置、额度展示与账号创建 / 编辑体验。
- 统一账户用量展示组件与提示交互，补充相关测试与中英文文案。

## 验证结果

- `bash scripts/verify-ci.sh release-gate`
- `go test ./internal/service/... ./internal/handler/...`
- `go test ./internal/repository/...`
- `go test -tags=integration ./internal/repository -run TestMigrationsRunner_IsIdempotent_AndSchemaIsUpToDate -count=1`
